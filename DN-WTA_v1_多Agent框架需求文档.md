# DN-WTA v1 多Agent独立决策与信息同步框架需求文档

| 项 | 内容 |
|---|---|
| 文档版本 | R1（2026-08-25） |
| 状态 | 已确认，作为下期开发依据 |
| 上游文档 | `DN-WTA_v1_数据集说明.md`（数据层，已完成）、`数据集规则.md` §11–§13、`DN-WTA_v1_下一步工作规划.md`（本文档对其目标分期进行改造） |
| 交付物 | `dwta/dn_env.py`（唯一新仿真环境）+ `dwta/test_dn_env.py`（六项验收单测） |
| 本期性质 | **纯环境层（框架期）**：不实现任何求解/训练算法 |

---

## 0. 本期四项已拍板决策（需求基线）

| # | 决策点 | 结论 | 主要影响 |
|---|---|---|---|
| C1 | 交付范围 | **纯环境层**：仅 DNEnv（`reset`/`step`/`get_observation`）+ 六项验收单测；无 runner、无落盘、无基线策略、无 CPLEX 接入 | 原规划 M1 后半与 M2 顺延至下期 |
| C2 | 观测口径 | **严格按数据集规则 §12.3（原始量）**：只暴露 `w_j`、`r_j(t)`、存活状态、`B_i`、`p_ij`、`d_ij(t)` 等原始量；`p_eff_ij(t)`、`h_ij(t)`、ETA 等派生量由算法侧自行计算 | 观测信息量最小最严格；原规划 §2.3"观测含派生量"方案废止（两口径信息等价，不构成泄密差异） |
| C3 | 同步机制 | **仅同步公共目标状态**：严格按 §11 Step 8，新出现/摧毁/突防三类公共状态全局广播；Agent 私有量（弹药/在途弹/动作/历史发射）永不同步；不做 Agent 间消息传递 | 无效交战（指标⑰）的机制来源得以保留 |
| C4 | 文档定位 | 正式需求结构 + 工程规格合一 | 本文档即开发与验收唯一依据 |

---

## 1. 背景与现状

**已完成（数据层）**：32 个 `data/dn_*.txt` 实例（m∈{3,5,10,20} × n∈{10,20,50,100} × 2 seeds）；`dwta/dn_instance.py` 纯标准库解析 + 派生动力学（`p_eff`、飞行步数、突防步），无仿真循环、无算法逻辑——正是为本期预留的地基。旧链路（`simulator.py`/`wave_runner.py`/`cplex/`）零改动冻结，E1–E4 可复现。

**问题重述（多Agent视角）**：数据文件是全局性数据集（Global Dataset），仅环境可完整读取；每个武器平台是独立 Agent，只能经信息边界获得局部观测，在 `Δt` 步内同时、独立决策；步末由环境执行一次公共目标状态同步。本期任务：把"独立决策 + 步末同步"机制做成可运行、可单测、可被未来所有算法（MARL/贪婪/启发式/LLM）统一复用的环境框架。

---

## 2. 目标改造：G1–G4 → G1'–G4'

按用户要求改造原规划 §0 四目标。**G2'、G3' 为本期核心；G1'、G4' 降级为"接口就绪/规范预留"。**

| # | 原目标 | 改造后目标（本期） | 本期验收标准 |
|---|---|---|---|
| G1' | 每平台=独立 Agent，可跑 MARL（原验收：IPPO/MAPPO 训练收敛） | **环境多Agent化就绪**：每平台以独立 Agent 暴露独立观测/动作空间/掩码；MARL 接入留待下期 | 六项单测全过；单测内以受控伪策略驱动 m 个 Agent 完成整局交互（无真实算法） |
| G2' | 全局信息+局部观测+步末同步 | 环境严格实现 §11 八步执行序与 §12.3 观测边界（C2 严格口径），有防泄密单测 | T5 通过；观测字段与 §6.3 字段表逐项一致 |
| G3' | 全局性系统约束 | 不变，只做环境+单测 | T1–T4 全部通过 |
| G4' | 多算法统一环境公平对比（原验收：同 runner 自动产出指标） | **框架预留**：`get_observation(i,t)` 成为所有分布式算法唯一合法信息通道（硬性规范）；Policy 接口与结果结构 v2 仅定义对接点 | 环境无绕过 `get_observation` 的信息出口（T5 断言）；`info` 字段足以离线计算指标 ①–⑱ |

---

## 3. 范围界定

**范围内**：(1) `dwta/dn_env.py` 环境核心；(2) `dwta/test_dn_env.py` 六项单测（unittest，标准库）；(3) 观测定版字段表（§6.3，核心交付）；(4) `get_share_observation(t)` CTDE 预留（同样受防泄密约束）；(5) 观测→定长向量编码约定（P2）。

**范围外（下期立项）**：统一 runner、结果结构 v2 落盘、`result_hash`（原 M1 后半/E5→M2'）；`dn_policies.py` 贪婪/启发式基线（→M2'）；CPLEX 上界接入（→M2'末/M3'）；IPPO/MAPPO（→M3'）；MARL+LLM（→M4'）；指标自动计算与表 1/表 2（→M5'）；`main.py` 新子命令；观测"完整档/消融档"配置。

---

## 4. 系统架构与信息流

### 4.1 三层信息结构

```
第 1 层  Global Dataset（data/dn_*.txt，已冻结）
         完整 k_arr/w_j/r0_j/p_ij/d0_ij/环境参数；仅 DNEnv 经 dn_instance 读取，算法永不可见
                        ▼ DNEnv 内部维护
第 2 层  Global State（仿真器私有，§12.2）
         t、全部目标状态（未出现/存活/摧毁/突防）、全部 B_i(t)、完整 p_ij/d_ij(t)、
         全部在途弹（含锁定 p_shot）、历史发射/结算 ⚠ 不等于任何算法可获得的信息
                        ▼ 信息边界（§12.3，C2 严格口径）
第 3 层  Local Observation O_i(t)（每 Agent 一份，§6.3 字段表）
         = 共享目标块（步末同步更新）+ 自身私有块 + 公共知识常量
         唯一出口：get_observation(i, t)
```

### 4.2 一个 time-step 的信息流（§11 八步序）

```
Step 1 目标出现（k_arr_j = t 进入）
Step 2 环境为每个 Agent 生成 O_i(t)（互不相同、互不可见他人）
Step 3 m 个 Agent 同时独立决策 a_i ∈ {0,1..n}（框架内=外部一次性输入）
Step 4 合法动作同时执行：锁定 p_shot、全局扣减 B_i
Step 5 时间推进 Δt，全部距离同步变化
Step 6 结算到达的在途弹（Bernoulli 按到达序；摧毁全局生效）
Step 7 突防判定（r_j≤0 ∧ 存活 → 突防，全局移除；同时刻先拦截后突防）
Step 8 信息同步：appear/destroy/leak 三类公共状态全局广播
       → 各 Agent 在 t+1 步使用更新后的公共目标状态
```

**同步白名单（C3，封闭式）**：新出现目标（ID）；目标被摧毁（ID+时刻）；目标已突防（ID+时刻）。

**永不同步黑名单（T5 断言）**：各 Agent 的 `B_i`、动作、历史发射；各 Agent 的在途弹（数量/目标/ETA/p_shot）；任何目标当前正被多少枚弹攻击；其他 Agent 的 `p_ij`、`d_ij(t)`。

> 设计依据：步内并行决策 + 私有量不同步，直接造成"多平台对同一目标过度覆盖 / 目标已毁后仍有弹在途"两类冗余消耗——这是原指标体系"无效交战率（⑰）"的机制来源，必须保留。

---

## 5. 全局性系统约束（G3' 落地口径）

在途弹与弹药虽是各 Agent 私有决策产物，但结算效果是全局的，由 Global State 统一执行：

| # | 约束 | 语义 | 单测 |
|---|---|---|---|
| S1 | 弹药全局扣减 | 发射即扣 `B_i←B_i−1`，跨步共享、永不恢复；`B_i=0` 时全部发射动作非法 | T1 |
| S2 | 摧毁全局生效 | 目标被结算摧毁后 Global State 立即标记，Step 8 广播；所有 Agent 下一观测一致；其剩余在途弹全部失效 | T2 |
| S3 | 突防全局移除+无效交战 | 目标突防即从交战名单全局移除；目标已摧毁/已突防后仍在途的弹失效：不结算、不计摧毁价值、弹药不返还 | T4 |
| S4 | 延迟结算 | 命中概率发射时刻锁定 `p_shot=p_eff(t_fire)`，飞行 h 步到达后按锁定值 Bernoulli（与结算时刻 `p_eff(t_hit)` 一般不等） | T3 |

---

## 6. 功能需求（FR）

### FR-1 环境生命周期与 API（`dwta/dn_env.py`）

```python
from dwta.dn_instance import DNInstance

class DNEnv:
    def __init__(self, inst: DNInstance, seed: int) -> None: ...

    def reset(self, seed: int | None = None) -> dict:
        """重置 Global State 至 t=0；返回 {"obs": [O_0..O_{m-1}],
        "share_obs": GlobalView, "info": {...}}。"""

    def step(self, actions: list[int]) -> tuple[list, list[float], bool, dict]:
        """输入 m 维动作（0=不发射，j+1=射向目标 j），按 §11 八步序执行一个
        决策步；返回 (obs_{t+1}, rewards, done, info)。rewards 为 m 维相同的
        全局团队奖励（FR-7）。done=True 仅在最后决策步+尾部结算完成后。"""

    def get_observation(self, i: int, t: int) -> "Observation":
        """Agent i 在步 t 的局部观测。所有分布式算法获取信息的唯一合法通道。"""

    def get_share_observation(self, t: int) -> "GlobalView":
        """CTDE 中心化视图（供后续 MAPPO critic）：全部 Agent 私有块拼接+共享块；
        同样禁止未出现目标信息。本期无消费方，仅保证存在且合规。"""

    def action_mask(self, i: int) -> list[bool]:
        """Agent i 当前合法动作掩码，长度 n+1。"""
```

- FR-1.1 非法动作（掩码外）**抛异常**而非静默忽略（框架期宁可硬失败）；
- FR-1.2 观测为不可变结构，环境不在 Agent 侧保留可变引用；
- FR-1.3 除 `get_observation`/`get_share_observation` 外无任何读取 Global State 的公开属性（`_` 私有化），杜绝绕过信息边界（T5 断言）；
- FR-1.4 仅依赖 `dwta.dn_instance` + 标准库，零新增第三方依赖。

### FR-2 八步执行序与尾部结算

**FR-2.1 常规步**：`step(actions)` 严格按 §4.2 顺序执行，要点：

- Step 3"同时决策"= 动作整步一次性传入，环境不提供任何步内交互或顺序信息；
- Step 6 多弹同一 `t_hit` 到达同一目标：按 `(t_hit, 发射序号)` 排序逐弹结算，先命中者有效，后续弹记无效交战（不重复计摧毁价值）；
- Step 7 同一时刻拦截弹到达与目标到边界并发：先拦截结算、后突防判定；
- 摧毁当步写入 Global State 并计奖励，经 Step 8 广播后对 Agent 下一决策步可见（步内不可见——并行决策语义）。

**FR-2.2 尾部结算（D1：结算）**：`t=K−1` 步执行完毕后，在途弹继续按 Step 5–6 推进结算直至 `max t_hit`（或无在途弹）：无决策步、无新发射、无信息同步、**不产生新突防**（期末存活不计泄漏）；尾部摧毁正常结算计入奖励；全部完成后在最后一个 `step` 返回 `done=True`（不产生额外调用）。

### FR-3 观测定版字段表（★本期核心交付，口径=C2 严格 §12.3，白名单制）

#### 6.3.1 共享目标块（所有 Agent 相同；随 Step 8 同步更新）

定长按槽位 `j=0..n−1` 编码，**未出现槽位全部字段强制为零**：

| 字段 | 形状 | 说明 |
|---|---|---|
| `t` | 标量 | 当前决策步（0..K−1） |
| `appeared_j` | n, bool | 已出现（`k_arr_j ≤ t`）且尚未移出 |
| `alive_j` | n, bool | 已出现 ∧ 未摧毁 ∧ 未突防 |
| `destroyed_j` | n, bool | 已摧毁（经 Step 8 广播；保留置位供记忆） |
| `leaked_j` | n, bool | 已突防（经 Step 8 广播；`r_j≤0 ∧ alive` 本身可由 `r_j(t)` 推出，无信息增量，决策 N3） |
| `w_j` | n, float | 目标价值（未出现槽位=0） |
| `r_j(t)` | n, float | 目标当前距防御边界距离（原始量；未出现/已移出=0） |
| `age_j` | n, int | 已出现步数 `t − k_arr_j`（未出现=0） |

#### 6.3.2 自身私有块（Agent i 独有，永不同步）

| 字段 | 形状 | 说明 |
|---|---|---|
| `B_i(t)` | 标量, int | 自身剩余弹药 |
| `p_ij` | n, float | 自身行命中概率参数（未出现槽位=0） |
| `d_ij(t)` | n, float | 自身与各目标当前距离（未出现槽位=0） |
| `own_inflight` | 列表 | 自身在途弹 `[(j, t_fire, t_hit), ...]`（仅自身可见） |
| `action_mask` | n+1, bool | 见 FR-5 |

#### 6.3.3 公共知识常量（初始化给定，不随 t 变）

`m`、`K`、`Δt`、`delta_d`、`v_m`、`p_cap`，及动作空间维度 `n+1`（结构性披露，决策 N2）。

#### 6.3.4 结构化→定长向量编码（P2，非验收依赖）

面向 NN 的定长向量 = 上述字段拼接（未出现槽位零填充），拼接顺序与归一化约定在 `dn_env.py` 顶部注释定版。验收只依赖结构化字段。

#### 6.3.5 防泄密红线（T5 断言完整清单）

观测（含 `share_obs`）禁止出现：(1) 未出现目标的任何非零字段；(2) 未来 `k_arr` 序列及"下一目标何时来"信息；(3) 其他 Agent 的 `B`/`p`/`d`/动作/历史发射/在途弹（`share_obs` 按定义是中心化视图，但仅限已出现目标）；(4) 全局在途弹计数/某目标正被几枚弹攻击；(5) 全局弹药总量 `ΣB_i`；(6) 预计算派生量 `p_eff_ij(t)`、`h_ij(t)`、预计突防时刻（C2：算法侧用公共公式自行计算）。

### FR-4 信息同步机制

- 时机：每决策步 Step 8，一次性全量（本期零延迟完美同步）；
- 内容：`appear/destroy/leak` 三类目标公共状态（§4.2 白名单）；
- 语义：写入各 Agent 下一观测的共享目标块；当步观测一经生成不再回写；
- 实现约束：同步以显式内部事件列表驱动（`sync_events` 随 `info` 暴露），为下期"同步延迟/部分同步"消融保留结构，**本期不实现任何延迟参数**。

### FR-5 动作空间与合法性掩码（D5）

- `Discrete(n+1)`：`0`=不发射，`j+1`=对目标 `j` 发射；每 Agent 每步至多 1 枚；
- 合法 = `appeared_j ∧ alive_j ∧ ¬leaked_j ∧ B_i>0`；
- **允许**对已有在途弹覆盖的目标再射（禁止即泄密：Agent 不应知道任何在途弹信息，覆盖冗余风险由算法自行权衡）。

### FR-6 奖励输出（D3 默认，全局团队奖励）

- `r_t = Σ(当步确认摧毁的 w_j) − Σ(当步突防的 w_j)`，所有 Agent 返回相同值；
- 发射成本项 `−λ·1[发射]` 预留参数，**默认 λ=0**，本期不做消融；
- 奖励仅为 API 完整性与下期训练预留，本期不做策略级验收。

### FR-7 确定性与 RNG 协议

- 环境内唯一 RNG：`random.Random(seed)`，`reset(seed)` 重建；
- 消耗顺序固定：按结算事件排序键 `(t_hit, 发射序号)` 逐弹 Bernoulli；环境内无依赖 Agent 遍历顺序的随机性；
- 同 seed 双跑**逐位一致**（T6）；决策顺序无关性由"动作整步传入"保证。

### FR-8 `info` 结构（结果结构 v2 的内存对接点）

每步 `info`（不落盘，仅返回值）：

```python
{
  "t": int,
  "actions": list[int],           # 原动作
  "legal": list[bool],            # 事后合法性
  "fires":  [{"i","j","t_fire","t_hit","p_shot"}],
  "settled": [{"k","i","j","killed","p_eff_at_settle"}],   # 含尾部结算步
  "leaked": [{"k","j","w_j","step"}],
  "sync_events": [{"type": "appear|destroy|leak", "k", "t"}],
  "destroyed_value_step": float, "leaked_value_step": float,
  "tail": bool,                   # 该结算事件是否发生在尾部阶段
}
```

设计目标：`info` 为结果结构 v2 `steps[]` 的超集，保证下期 runner 落盘后指标 ①–⑱ 全部离线可算（如⑰无效交战率 = `settled` 中目标已毁/已突防的弹数 / 总发射数）。

### FR-9 扩展预留（本期仅定义，不实现）

- **Policy 统一接口签名**（下期 M2' 实现）：`act(obs_i, action_mask, rng) -> int`（随机式）/`act(obs_i, action_mask) -> int`（确定性）；环境侧不感知 Policy 类型；
- **多算法接入点**：所有算法仅经 `reset → 循环{ get_observation → 决策 → step }` 驱动环境，G4' 公平性由此结构保证；
- **share_obs 消费方**：MAPPO critic，接入时再定格式。

---

## 7. 非功能需求

| # | 需求 | 说明 |
|---|---|---|
| N-1 | 零新增依赖 | 仅标准库（含 `random`）+ `dn_instance.py` |
| N-2 | 可测试性 | 六项单测不依赖任何外部文件产出；用固定小实例（如 `data/dn_m3_n10_*.txt`）+ 构造种子驱动 |
| N-3 | 性能（软指标） | 最大实例 m20×n100 单 `step` < 1ms（不设硬门槛，为下期在线 runner 的 Δt=2s 预算留余量） |
| N-4 | 旧链路零改动 | `simulator.py`/`wave_runner.py`/`cplex/` 冻结只读；DNEnv 为唯一新开发对象；两链路结果不混排 |
| N-5 | 规范合规 | 遵守 `项目整理规范.md`：无新顶层文件夹、新增文件平铺于 `dwta/`、从项目根目录运行 |
| N-6 | 可复现 | 同实例+同 seed+同动作序列 → 完全一致轨迹（含 RNG 消耗顺序） |

---

## 8. 验收标准：六项单测（`dwta/test_dn_env.py`，unittest）

运行方式：项目根目录 `python -m unittest dwta.test_dn_env -v`。**六项全绿 = 本期验收通过。**

| # | 名称 | 驱动方式 | 断言要点 |
|---|---|---|---|
| T1 | 弹药全局扣减 | 伪策略连射直至打空 | (a) 每次发射后 `B_i` 恰减 1 且跨步不恢复；(b) `B_i=0` 后发射动作掩码全灭，强行提交抛异常；(c) 不发射时 `B_i` 不变 |
| T2 | 摧毁全局生效 | 种子扫描至出现"当步摧毁" | (a) 摧毁当步 Global State 标记（经 `info.settled` 验证）；(b) **下一观测中所有 Agent 的 `destroyed_j=1` 完全一致**；(c) 该目标后续距离冻结、不再可被选中 |
| T3 | 延迟结算 | 构造 `t_fire` 与 `t_hit` 跨步的发射 | (a) 发射时刻 `info.fires` 记录 `p_shot=p_eff(t_fire)`；(b) 结算用锁定值（以可控 RNG 种子验证 Bernoulli 结果与锁定值一致、与 `p_eff(t_hit)` 无关）；(c) 含一例 `t_hit≥K` 的尾部结算仍生效且计入奖励 |
| T4 | 无效交战 | 多 Agent 对同一目标齐射+等待摧毁 | (a) 目标已毁后其余在途弹 `settled.killed` 恒 False 且不计摧毁价值；(b) 目标突防后在途弹同样失效；(c) 所有情况弹药均不返还；(d) 尾部阶段无新 `leaked` 事件 |
| T5 | 防泄密 | 全实例扫描式驱动整局 | (a) 任一步、任一 Agent：`appeared_j=0 ⇒ w/r/p/d/age` 该槽位全零；(b) 观测字段集合 ⊆ §6.3 白名单（序列化后键名逐一比对）；(c) 他人私有量与全局在途弹计数不可达；(d) `dir(env)` 无公开 Global State 出口；(e) `share_obs` 不含未出现目标非零字段 |
| T6 | 确定性可复现 | 同实例同 seed 双跑（含随机伪策略） | (a) 全轨迹（观测、奖励、done、info）**逐位一致**；(b) 不同 seed 至少一处分歧（防"假确定性"——RNG 未被消耗） |

> T2(b) 是"信息同步机制"的直接验收：摧毁事件经 Step 8 广播后，各 Agent 共享目标块收敛一致。

---

## 9. 本期新增设计决策（编号衔接原规划 D1–D8）

| # | 决策 | 理由 |
|---|---|---|
| N1 | 观测口径取严格 §12.3 原始量（C2） | 信息量最小最严格；派生量（`p_eff` 等）可由原始量+公共参数推出，算法侧计算不改变信息集合，后续如需"完整档"再立项 |
| N2 | 动作空间维度 `n+1` 属结构性公共信息 | 定长动作空间不可避免披露 n；防泄密语义以"未出现槽位零填充+掩码"保证——观测/奖励中不显式提供 n 或未来目标数，规则算法同样不使用未出现槽位。论文口径中如实说明 |
| N3 | `leaked_j` 并入公共状态广播 | §12.3"存活/摧毁状态"的等价推广；`r_j≤0 ∧ alive` 本身可由已暴露的 `r_j(t)` 推出，无信息增量；同时使"突防全局移除"（S3）对 Agent 可见，避免死目标持续占位 |

---

## 10. 目录与文件规划（遵守项目整理规范）

```
WTA-Dn-branch01/
├── dwta/
│   ├── dn_env.py          # 新增：DNEnv 环境核心（FR-1..FR-9）
│   └── test_dn_env.py     # 新增：六项验收单测（T1..T6，unittest）
└──（其余目录与文件零改动）
```

- 无新顶层文件夹，符合"一个顶层子文件夹=一个模块"准则；
- 依赖方向：`dwta/test_dn_env.py → dwta/dn_env.py → dwta/dn_instance.py`（单向，无循环）；
- 本文档存放主目录（总结性质文档一律放主目录）。

---

## 11. 里程碑与任务分解（本期，预计 1–1.5 周）

| 任务 | 内容 | 产出 | 对应 |
|---|---|---|---|
| W-a | `dn_env.py` 骨架：构造/reset/Global State/`get_observation` | 观测生成可用 | FR-1, FR-3 |
| W-b | 信息边界先行：T5 初版（测试驱动字段白名单） | T5 通过 | G2' |
| W-c | `step` 八步序 + 全局约束 S1–S4 | 完整决策步 | FR-2.1, FR-5, FR-6 |
| W-d | T1–T4 编写与修正 | 四项单测通过 | G3' |
| W-e | 尾部结算 + RNG 协议 + `info` 结构 | FR-2.2, FR-7, FR-8 | G4' 预留 |
| W-f | T6 + 全量回归 + 文档收尾（README 增补一行说明） | 六项全绿，验收通过 | 全部 |

**后续阶段概览**（另行立项，不在本期）：M2' 基线策略+统一 runner+结果 v2 落盘（E5 全量）→ M3' MARL（IPPO/MAPPO）→ M4' MARL+LLM → M5' 全算法对比与报告。原 M1/M2 内容相应顺延。

---

## 12. 风险与开放问题

**风险**：

1. *严格观测口径增加后续 MARL 特征工程成本* → 缓解：`p_eff` 等为公共公式，算法侧自行计算成本可控；接口按"字段只增不减需版本化"原则设计，未来"完整档"扩展不破坏本期框架；
2. *动作空间结构性披露 n* → 已明示为决策 N2，论文中如实说明，不影响信息边界核心语义；
3. *新旧仿真并存口径漂移* → DNEnv 为唯一新开发对象，`simulator.py` 冻结只读，两链路结果不混排；
4. *单人开发无评审* → 单测即评审，六项全绿为硬门槛，禁止跳过。

**开放问题（不阻塞本期）**：

- 向量编码的归一化方案（留给算法侧，编码布局本期定版）；
- `share_obs` 的 critic 消费格式（MAPPO 接入时再定）；
- 多弹同刻到达的结算顺序（已定发射序）对指标⑰的微小影响，需在论文口径中说明。
