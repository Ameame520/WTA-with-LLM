# 实验报告（终版）：DN-WTA 全策略对比与 marl 学习策略

> 版本：v1.1（2026-09-03）
> 本报告为 DN-WTA 系列实验的**唯一权威总结**，内容覆盖并替代以下三份既有报告：
> 《实验报告_DN算法对比.md》《实验报告_DN学习策略.md》《实验综合报告.md》。
> **数字来源约定**：本报告一切结果数字的唯一来源为 `output/` 目录下的实测产物
> （`family_report.json` / `train_log.jsonl` / `train_summary.json`），未采信任何文档转述值；
> 少量历史背景数字（v2 时代 E5–E10）引自 `LOGS_SUMMARY.md` 并明确标注。

---

## 1. 问题设定与实验系统

### 1.1 场景与动力学（数据集 `dn-data-v3` 锁定参数）

| 项 | 值 |
|---|---|
| 平台/目标/步数 | m=3 平台（=智能体数），n=50 目标，K=10 决策步 |
| 弹药 | μ=6/平台，**全局共享池 18 发**，整局不补给，每平台每步至多 1 发 |
| 时间 | dt=10 s/步；目标每步逼近 δ_d=3 km；拦截弹速 v_m=1 km/s |
| 杀伤概率 | p_eff = min(0.95, p_ij·d0_ij/d_ij(t))，发射时锁定，弹着步（t+h）伯努利结算 |
| 泄漏 | r_j(t)≤0 即突防泄漏（全额价值）；t=K 仍存活计期末泄漏 |
| 决策窗 | t ∈ [0, K−2] |
| 数据 | 30 实例固定划分：test=s01–s02、train=s03–s26、val=s27–s30（`MANIFEST.md` 锁定） |
| 实例总价值 | s01=3942，s02=3819（test）；w 时序趋势 1.5，r0 配额 1,1,3 |

任务概述：**目标分批到达，三个平台共享 18 发弹药；每个决策步各平台独立决定对哪个目标发射或保持待机；拦截弹经数步飞行后到达，命中结果按发射时锁定的概率作伯努利结算；目标剩余突防时间耗尽即造成价值泄漏。优化目标为最小化泄漏目标价值总和。**

### 1.2 信息边界（理解 marl 设计的前提）

每个平台每步可获取的信息限于以下三类：

1. **共享态势**：所有已出现目标的状态（价值 w_j、剩余突防时间 r_j、存活标志、出现年龄）；全局弹药池水位 B(t)；
2. **平台私有量**：自身到各目标的距离 d_ij、基础杀伤概率 p_ij、已发射的在途拦截弹（交战目标、到达步、锁定概率）；
3. **实例公共先验**：动力学参数（v_m、δ_d、dt、K、pcap）与目标价值表——按 greedy 策略先例视为公共知识。

**不可见信息**：未来目标到达序列、其他平台在途拦截弹的去向、以及任何环境内部真值。这条边界（专利文档 §7）是 marl 执行路径的硬约束：所有输入必须由上述三类信息派生。

### 1.3 参与对比的策略族

| 策略 | 定位 | 信息口径 |
|---|---|---|
| `none` | 无防御下界 | 不动作 |
| `greedy` | 分布式局部贪心 | 仅 §7 观测（score = w·p_eff/剩余窗） |
| `cplex` | 集中式短视最优（上界参考 + gap 分母） | 全知（故意突破 §7，作参考解源） |
| `marl` | **本项目交付的 CTDE 学习策略**（六模块 M1–M6，28482 参数） | 执行路径仅 §7 观测 + 公共先验 + 自身记忆 |

### 1.4 评估协议与指标

- 终评：test split（2 实例）× 30 MC seeds（42–71），实例 × seed 全遍历，四个策略完全同协议；
- 指标：泄漏率（核心）、逐步最优性 gap（对逐步 CPLEX 参考解）、无效交战率⑰、弹药效率（毁伤值/发）、决策时延⑱ p50/p90、发射数、毁伤价值，全部由 `experiments/dn_report.py` 统一产出；
- RL 专属指标：⑨收敛曲线（`train_log.jsonl` 每 20 iter 一评）、⑩泛化性 D = train均值 − test 均值、⑪训练成本（墙钟 / env_steps / 参数量）。

---

## 2. 学习型策略 marl 的设计与实现

marl 是一个**多智能体集中训练、分散执行**（CTDE）的策略：训练阶段由可访问全局真值的中心化价值网络（critic）辅助更新，执行阶段各平台仅依赖自身观测独立决策，没有通信与中心节点。

### 2.0 总体数据流

**执行链（评估/部署，每步每平台）**：

```
obs_i = env.get_observation(i, t)        # ① 取 §7 观测
mem_i.update(obs_i, t)                   # ② M1 记忆递推（λ 重构）
feats  = build_inputs(obs_i, mem_i, dyn) # ③ M1 特征打包（10/5/3 维三组）
logits = MarlNet(feats)                  # ④ M2 注意力编码 + M3 打分
action = argmax(mask(logits))            # ⑤ M4 合法性屏蔽后取最优（含"弃射"）
mem_i.note_own_shot(...)                 # ⑥ 私有记忆登记自身发射
```

**训练链（每 episode）**：同一 actor 路径逐步采集 (特征, 动作, logp)，同时训练侧 wrapper 额外缓存全局真值供 critic；episode 结束后从环境事件记录离线构造逐步奖励（M5），再做 PPO 更新（M6）。

以下逐模块展开。

### 2.1 M1 感知重构：友军火力承诺度的分布式估计（`marl/perceive.py`）

**要解决的问题**：观测中不包含友军平台的在途拦截弹信息，而各目标当前的火力承诺程度直接决定补射的边际价值——多发拦截弹指向同一目标时，后续各发的边际毁伤期望递减。M1 的任务是在 §7 信息边界内，仅利用公共量与自身记忆，为每个存活目标估计**友军火力承诺度 λ_j(t)**。

**实现方式**：每个平台维护一份递推记忆（`AgentMemory`）。公共信息中唯一可用的火力线索为全局弹药池水位差分：

```
n̂(t)    = B(t−1) − B(t)                          # 上一步全队发射总数（公共量）
λ_j(t+1) = 0                                       若目标 j 本步阵亡/泄漏（alive 翻转）
        = 0.6·λ_j(t) + (n̂(t) − a_i(t))⁺ / |C(t)|   否则
```

其中 a_i(t) 为本平台上一步的发射数（私有量，精确已知；扣除后剩余部分即友军发射量），C(t) 是当前存活且本步未翻转的目标集合。其构造方式为：将友军发射部分 (n̂(t)−a_i(t))⁺ 均匀分摊至当前全部存活目标，并以 0.6/步的速率指数遗忘。该估计不区分友军火力的具体去向，仅利用发射总数与存活目标数，属于保守的均值界估计；满足有界性（Σ_j λ_j ≤ 全池 18 发），且目标状态翻转（阵亡/泄漏）时立即清零。

**特征打包**（`build_inputs`，输入仅 obs + 记忆 + 实例公共先验，函数签名不接收 env）：

| 张量 | 维度 | 内容（逐项） |
|---|---|---|
| x_j（每存活目标一行） | 10 | w_j/总价值、r_j/r_j(0)、剩余交战窗 u_j=⌈r/δ⌉、age/K、飞行步数 h_ij、当前有效杀伤概率 p̄_ij、**λ_j/18（友军火力承诺估计）**、可行性余量 u_j−h_ij、本平台在途弹中指向 j 的数量、上步池差分 n̂(t) |
| q_i（平台自身） | 5 | 池水位/18、自身在途弹总数/18、自身距离行摘要（均值 d、最大 d，各/100）、t/K |
| g（全局） | 3 | 池水位/18、t/K、当步翻转目标数 |

一处实现说明：规格中的"平台位置 (x,y)"在本环境不可直接观测（几何信息仅通过距离矩阵 d_ij 侧面可见），故以距离行的两个摘要统计量替代——这是观测结构下合理的位置代理特征。

### 2.2 M2 集合注意力编码：目标数量可变的置换不变编码（`marl/network.py`）

**要解决的问题**：可见目标数量随时间动态变化（0 至 50），且目标在输入中的排列顺序不应影响决策结果。

**实现方式**：将每个存活目标行 x_j 投影为 64 维 token，连同平台自身（q_i 投影）与全局状态（g 投影）两个额外 token，一并送入**一层 2 头多头自注意力**（d=64）。注意力机制天然满足置换不变性（selftest 以随机重排断言逐目标打分一一对应，容差 1e-5）。输出三个部分：平台嵌入 z_i、每个目标的键值对 (k_j, v_j)、全局嵌入 z_g。

### 2.3 M3 决策头：目标交战打分与弃射选项（`marl/network.py`）

对每个目标计算交战分，同时为"当前步保持待机"单独计算一个弃射分：

```
s_j = MLP₂([z_i; k_j; z_i ⊙ k_j])    # 192→32→1，交互式打分（平台×目标逐元素积捕捉匹配度）
s_⊥ = MLP₂([z_i; z_g])               # 128→32→1，弃射分（仅依赖自身与全局态势）
分布 = softmax([s_⊥, s_1 … s_L] / τ)  # hold 位固定在 index 0
```

训练期按温度 τ 采样探索（τ 从 1.0 线性退火至 0.5），**执行/评估期一律 argmax**——这保证了部署行为的确定性（同 seed 同结果）。整个网络可训练参数 **28 482** 个，启动时断言 ∈[1e3, 1e5]。

### 2.4 M4 合法性屏蔽：从源头消除非法动作（`marl/masking.py`）

打分之后、选择之前，先将不可行动作全部屏蔽为 −inf。一个目标可交战需同时满足四个条件：

- **目标存活**：目标当前处于存活状态；
- **交战窗口可达**：飞行步数 h_ij ≤ 剩余交战窗 u_j（弹着不晚于目标突防）；
- **弹药可用**：全局池 B(t) > 0；
- **结算可达**：t + h_ij ≤ K。环境结算循环止于 t=K，弹着晚于此的弹永不结算且弹药不返还（greedy/cplex 均无此过滤，是已知的弹药浪费源）；marl 主动加严此条件，属于自利的正确性保护。

"弃射" ⊥ 永远合法。效果：全部评估中 30 seeds × 2 实例的逐 run 记录 `illegal_actions` **恒为 0**（selftest 还断言了每个 mask=True 的动作都通过环境 `can_fire` 合法性检查）。

### 2.5 M5 奖励与信用：稀疏事件流 + 势函数塑形 + 击杀归因（`marl/reward.py`）

**要解决的问题**：单局仅 10 个决策步，结算事件（击杀/泄漏）数量少且集中在弹着步，直接以团队总回报作为梯度信号面临奖励稀疏与反馈延迟问题。

**三项设计**（全部从环境的事件记录离线回放构造，环境本身零改动）：

1. **事件流团队奖励**——事件在发生步即时计入：
   `R(t) = Σ_{泄漏@t}(−w_j/total) + Σ_{击杀@t}(+w_j/total) − 0.01×无效结算数@t`
2. **势函数塑形**——为在途弹的期望毁伤提供即时反馈，无需等待命中结算：
   `Φ(s) = −Σ_{存活 j} w_j·p̄_j/total`，其中 p̄_j = 1−∏(1−p_shot) 为该目标全部在途弹的合击概率；
   `R′(t) = R(t) + 0.99·Φ(t+1) − Φ(t)`。塑形项不改变最优策略，只加密学习信号。
3. **击杀信用归因**：每发结算结果为 kill 的拦截弹，其 +w_j/total 计入发射槽 (t_fire, i)，即归因到具体决策步与发射平台。

**对账自检**（每次运行断言）：Σ_t R_team 与 run_rec 聚合量（毁伤−泄漏，归一口径）误差 < 1e-9；kill 弹数 = 环境毁伤数；credit 键集 ⊆ 环境发射记录键集。

### 2.6 M6 CTDE 训练：MAPPO 式 PPO + COMA 式反事实基线（`marl/train.py`）

**分工**：actor（即上述 MarlNet）执行时仅依赖 §7 观测；critic 仅存在于训练侧，可访问全局真值——在途弹登记表、每目标真实火力占用、池水位、当前步——对联合动作的价值进行中心化评估。**全局信息只进 critic，永不进 actor**，这是分散执行能力的保证。

**critic 结构**：V(s, a) = MLP(44→128→1, Tanh)，输入拼接三块：
- 存活目标真值行池化（每目标 5 维：w/total、r/r0、真实占用数/6、合击概率 p̄、剩余窗/10）；
- 全局 3 维（池水位、t/K、存活比例）；
- 3 平台 × 3 维动作摘要（是否弃射独热、所选目标价值、p_eff）。

**优势估计**：`A_i(t) = GAE_team(t) + 击杀信用(t,i) + [V(s,a) − V(s, a_{−i}, ⊥_i)]`——最后一项是 COMA 式反事实基线：将智能体 i 的动作替换为弃射前后联合价值的差值，即该智能体此步动作的边际贡献。

**PPO 超参**（训练脚本锁定）：

| clip | 熵系数 | GAE λ | γ | lr | episodes/iter | PPO epochs | minibatch |
|---|---|---|---|---|---|---|---|
| 0.2 | 0.01 | 0.95 | 0.99 | 3e-4 (Adam) | 32 | 2 | 256 |

**种子隔离**：训练采集 seed 从 100001 递增；早停验证 val = s27–s30 × seeds 42–51（只算泄漏率，不调 CPLEX）；终评另用 42–71——三条随机流互不污染。

**早停**：每 20 iter 评一次 val 泄漏率，连续 20 个评估点无改善即停，取历史最优 checkpoint 为 `best.pt`。

### 2.7 工程与可靠性设计

| 特性 | 实现 |
|---|---|
| 评估确定性 | 执行/终评一律 argmax（不采样），同 seed 同 `result_hash`，可逐位复现 |
| 随机流隔离 | 策略采样使用独立 `torch.Generator`，不读取、不消耗 `env.rng`——环境蒙特卡洛结算流不受策略影响 |
| 跨 seed 记忆安全 | `act` 检测时间步回绕（新 episode 的 t ≤ 上次 t）自动重置 λ 记忆，30 seeds 循环复用同一策略对象而不产生跨 seed 记忆污染 |
| 设备自适应 | MPS 优先，前向异常自动降级 CPU（try/except 在位，日志可见） |
| 环境零侵入 | `dwta/dn_env.py`、`dn_instance.py` 未做任何改动；奖励/信用全部从 `env.shots` / `env.leaked_at` / `env.destroyed_at` 事后离线读取 |
| 求解器隔离 | CPLEX 仅按既有子进程协议调用，算法零改动 |
| 依赖 | 仅 torch 2.13.0 + numpy 2.2.6，无新增第三方包 |
| 自检体系 | 五个模块文件均内嵌 selftest：λ 有界/翻转清零/输入纯度（perceive）、置换一致性（network）、屏蔽合法性（masking）、3 seeds 冒烟 illegal=0 且 act<1s（policy）、奖励对账 <1e-9（reward） |

---

## 3. 实验一：三基线复现与锁定（E11–E13）

在 marl 开发前后一致性验证中，用同一管线重跑三个基线并锁定数值（test × 30 seeds，timelimit 30s，产物 `output/regress_*`，2026-09-02）：

| 实验 | 策略 | 泄漏率（实测） | gap | 无效交战率 | result hash（s01/s02） |
|---|---|---|---|---|---|
| E11 | none | 1.0000±0.0000（bt 38 + 期末 12） | n/a | 0 | `978e41ee12da` / `29faf747e90e` |
| E12 | greedy | 0.7913±0.0095（worst 0.8008） | 0.2768±0.0115 | 0.3472 | `d96c7504044d` / `3446b7a8f986` |
| E13 | cplex | 0.7085±0.0343（worst 0.7428） | 0.0000（自洽） | 0.0963 | `df512d01e503` / `96d35c879acd` |

三行数值与 `LOGS_SUMMARY.md` 历史记录逐字段一致，证明环境与求解链路稳定可用。

**历史背景（v2 时代单实例对照，引自 LOGS_SUMMARY）**：v2 数据上 greedy 0.5757±0.0449 / 0.5358±0.0304，cplex 0.4917±0.0476 / 0.4375±0.0414（gap 恒 0），none 1.0000。v3 因 w 时序趋势 1.5 + 配额 1,1,3 显著加难（cplex 从 0.49 → 0.71，远离理论上界），前瞻/反应式策略的改进空间被刻意拉开——这是 v3 的设计意图（数据集说明 §10.1），也构成学习策略的动机背景。

---

## 4. 实验二：marl 学习策略训练与终评（E14）

### 4.1 训练过程（⑨，`output/e14_marl_train/`）

- 配置：MAPPO + COMA 式反事实基线，MPS 设备，τ 1.0→0.5 退火（本次运行因早停终止于 0.885），val = s27–s30 × seeds 42–51 泄漏率早停，patience = 20 评估点，每 20 iter 一评。
- 曲线（`train_log.jsonl` 全 23 点）：val 从 iter 20 的 0.9366 降至 **iter 60 的 0.8943（全局最优）**，此后 20 个评估点（至 iter 460）未再改善（iter 380 起进入平台期，稳定于 0.914 附近），触发早停；train_leak 全程在 0.82–0.88 间波动；policy_loss 在 ±1.8 间震荡（稀疏奖励下的高方差）。
- **达到 val≤0.80 所需 iter：未达到**（best 0.8943 > 0.80）。

### 4.2 训练成本（⑪，`train_summary.json`）

| 项 | 值 |
|---|---|
| 总墙钟 | 5917.5 s ≈ **1.64 h** |
| env_steps | 132 480 |
| params_count | **28 482** |
| 收尾 | 460 iters / 23 eval 点 / early_stop / mps |

### 4.3 test 终评主表（`output/e14_dn3_marl/`）

四策略同表（test × 30 seeds，family 级均值±std，全表实测）：

| 指标 | none | **marl** | greedy | cplex |
|---|---|---|---|---|
| **泄漏率** | 1.0000±0.0000 | **0.9009±0.0295** | 0.7913±0.0095 | 0.7085±0.0343 |
| worst 实例均值 | 1.0000 | 0.9304 | 0.8008 | 0.7428 |
| gap（vs 逐步 CPLEX） | n/a | 0.7032±0.0361 | 0.2768±0.0115 | 0.0000 |
| 无效交战率⑰ | 0.0000 | **0.1130±0.0889** | 0.3472±0.0676 | 0.0963±0.0537 |
| 弹药效率（毁伤值/发） | n/a | 21.75±6.65 | 46.65±0.71 | 67.92±3.22 |
| 决策时延 p50/p90 (s)⑱ | 0/0 | 0.468/0.516（含参考解；纯前向 ≈0.004） | 0.439/0.513 | 0.383/0.437 |
| 发射数 | 0 | 18（全部发射） | 18（全部发射） | 18（全部发射） |
| 毁伤价值均值 | 0 | 386.4±120.7 | 809.2±24.0 | 1129.1±115.0 |

marl 分实例（30 seeds 均值）：

| 实例 | 泄漏率 | gap | invalid | 毁伤价值 | result hash |
|---|---|---|---|---|---|
| s01（总值 3942） | 0.8713 | 0.6671 | 0.2019 | 507.2 | `3931af8e9607` |
| s02（总值 3819） | 0.9304 | 0.7394 | 0.0241 | 265.7 | `ae69d2700fd8` |

### 4.4 泛化性（⑩，`output/e14_generalization/`，`--no-ref` × 30 seeds）

| split | 泄漏率 | D（train−test） |
|---|---|---|
| train（s03–s26，24 实例） | 0.9166±0.0247（worst 0.9540） | **+0.0157** |
| val（s27–s30，4 实例） | 0.8902±0.0134 | — |
| test（s01–s02） | 0.9009±0.0295 | — |

三 split 均值高度一致（D = +0.016，远小于 0.10 的过拟合警戒线；train split 实例因 w-trend 生成口径略难，均值反而最高），**策略学到的是数据集的共性规律而非实例记忆**。无参考解负载下的真实前向时延：train 0.004 s / val 0.006 s（p50）。

### 4.5 行为特征读数

- **弹药全部发射但毁伤密度不足**：三个交战策略均发射全部 18 发，但 marl 单发期望毁伤仅 21.8（greedy 46.7、cplex 67.9）——差距主要来自目标选择质量（gap 0.70 vs 0.28），而非发射量。
- **交战有效性优于 greedy**：无效交战率 0.113 显著低于 greedy 的 0.347（接近 cplex 的 0.096），表明 M4 屏蔽与 λ 协同估计有效抑制了重复交战与过晚交战。
- **实例间方差大**（invalid 0.02–0.20、毁伤 266–507），策略对实例扰动敏感，与训练仅见 24 实例的容量一致。
- **部署可用性**：零非法动作 + 纯前向毫秒级时延（4 ms ≪ 单步预算 10 s），满足在线部署的硬条件。

---

## 5. 结论

### 5.1 总结论

1. **框架交付完整可用**：六模块 M1–M6 全部落地并通过各自内嵌自检；评估管线、种子隔离、确定性复现、设备自适应等工程特性齐备；训练-终评-泛化全链路产物（`best.pt` / `train_log.jsonl` / `train_summary.json`）完整。
2. **学习策略达到第一版基线水平**：test 泄漏率 0.9009±0.0295，显著优于无防御（−10 个百分点），尚未追平分布式贪心（0.7913，差 0.110）；交战有效性（无效交战率 0.11 vs 0.35）已优于 greedy。
3. **部署与泛化特性良好**：零非法动作、纯前向 p50≈4 ms、三 split 泄漏率一致（D=0.016）。

### 5.2 与 greedy 差距的归因（基于实测证据）

1. **信用稀疏**：团队奖励中唯一强正信号是击杀（命中率约 50%），且击杀信用归因到 (t_fire, i) 后方差极大；势塑形提供的密集信号仅覆盖"在途期望"，无法区分目标选择优劣。
2. **训练预算内未收敛**：val 曲线在 iter 60 后进入平台期（0.894→0.914 区间震荡），早停在曲线仍在缓慢下降时触发——1.64 h / 132k env_steps 的实际训练量明显偏少（patience 评估点口径下 400 iter 即停）。
3. **策略偏保守且分散**：18 发弹药全部发射，但毁伤价值与弹药效率均偏低——"何时交战高价值目标"的序贯决策（gap 主因）未被充分学习。

### 5.3 后续方向（优先级序）

1. **奖励重整**：击杀信用改为按在途份额摊派或引入 per-shot 边际项；c_invalid 可试上限 0.05，抑制 s01 上 20% 的无效率。
2. **训练预算与早停放宽**：patience 评估点 20→40+，或 eval 间隔 20→10 iter；目标 val ≤0.85（第二版）→ 0.80。
3. **消融路线**：λ 特征消融重训，验证 M1 贡献（诊断 λ 与真实火力占用列的相关系数）。
4. **前瞻优势空间**：v3 设计意图显示短视 cplex（0.71）距理论上界尚远，具备序贯决策能力的学习策略是唯一有希望同时压缩 gap 与泄漏率的路线。

### 5.4 复现命令（全部产物可一键重建）

```bash
PY=/opt/anaconda3/envs/wta/bin/python   # torch 2.13.0 / numpy 2.2.6 / MPS

# 三基线
$PY experiments/dn_family_eval.py --split test --policy none  --seeds 30 --output output/regress_e11_none
$PY experiments/dn_family_eval.py --split test --policy greedy --seeds 30 --output output/regress_e12_greedy
$PY experiments/dn_family_eval.py --split test --policy cplex --seeds 30 --timelimit 30 --output output/regress_e13_cplex

# E14a 训练 → E14b 终评 → 泛化快照
$PY marl/train.py --iters 100000 --eval-every 20 --patience 20 --device auto --output output/e14_marl_train
$PY experiments/dn_family_eval.py --split test --policy marl --model output/e14_marl_train/best.pt --seeds 30 --output output/e14_dn3_marl
$PY experiments/dn_family_eval.py --split train --policy marl --model output/e14_marl_train/best.pt --seeds 30 --no-ref --output output/e14_generalization/train_split
$PY experiments/dn_family_eval.py --split val   --policy marl --model output/e14_marl_train/best.pt --seeds 30 --no-ref --output output/e14_generalization/val_split

# 模块自检
$PY marl/perceive.py --selftest && $PY marl/network.py && $PY marl/masking.py && $PY marl/policy.py
$PY marl/reward.py --selftest --instances data/dn-data-v3/dn_3x50_K10_s01.txt,data/dn-data-v3/dn_3x50_K10_s05.txt
```

> 运维注：CPLEX 参考解长跑（每步删 2 个临时文件）曾受工作区安全删除阈值影响中断，
> 规避方式为 `--output /tmp/<dir>` 跑完后拷回。

---

## 附：产物索引

| 目录 | 内容 | 关键结果 |
|---|---|---|
| `output/regress_e11_none/` | none 基线 | leak 1.0000；指纹 `978e41ee12da`/`29faf747e90e` |
| `output/regress_e12_greedy/` | greedy 基线 | leak 0.7913±0.0095；gap 0.2768 |
| `output/regress_e13_cplex/` | cplex 基线 | leak 0.7085±0.0343；gap 0.0000 |
| `output/e14_marl_train/` | 训练产物（best.pt / train_log.jsonl / train_summary.json） | 460 iters 早停 / 1.64 h / 28482 参数 / best val 0.8943 |
| `output/e14_dn3_marl/` | test 终评（含参考解） | leak 0.9009±0.0295；gap 0.7032；纯前向 p50 4 ms |
| `output/e14_generalization/{train,val}_split/` | 泛化快照（--no-ref） | train 0.9166 / val 0.8902 / test 0.9009，D=0.0157 |
