# DN-WTA v1 数据集说明

> 生成日期：2026-08-25 ｜ 规范依据：`数据集规则.md`（DN-WTA v1）
> 生成器：`experiments/gen_dn_data.py` ｜ 解析器：`dwta/dn_instance.py`（纯标准库数据层）
> 数据位置：`data/dn_*.txt`（32 个文件）

---

## 1. 数据集定位

DN-WTA v1 是面向**多智能体算法对比**的动态武器目标分配数据集，在旧版 `dyn_*` 数据基础上引入：

1. 目标分时到达（`k_arr`）+ 持续逼近（每步 `delta_d` km）；
2. 平台异质初始距离矩阵 `d0_ij`（不同武器对同一目标距离不同）；
3. 杀伤概率随距离动态演化（`p_eff = min(pcap, p_ij·d0_ij/d_ij(t))`）；
4. 弹药**全程总额**约束（`mu` 为整个任务周期的初始弹药，跨时间步不恢复）；
5. 拦截弹飞行时间与**延迟命中结算**（发射时锁定 `p_shot`，飞行 `h` 步后 Bernoulli 结算）；
6. 全局状态 / 局部观测分离（数据文件仅由仿真环境读取，算法经 `get_observation(i,t)` 取信息）。

与旧数据的区别一句话概括：旧 `dyn_*` 是"逐波重置弹药 + 当波立即结算 + 共享距离"的波次仿真；DN v1 是"全程弹药 + 延迟结算 + 平台异质 + 信息边界"的多智能体时序环境。

## 2. 文件格式

纯 `.txt` 数值格式，总行数 `1 + n + 2*m*n`：

```text
第 1 行            : m n K mu dt delta_d v_m pcap        （Header，8 字段）
随后 n 行          : k_arr w_j r0_j                      （Target Block，每目标一行）
随后 m*n 行        : i j p_ij                            （Probability Block）
随后 m*n 行        : i j d0_ij                           （Distance Block）
```

示例（`dn_3x10_K10_s1.txt` 前 4 行 + 末 2 行）：

```text
3 10 10 1 2.0 1.0 1.5 95
2 20 5
9 49 8
1 91 7
...
2 8 18
2 9 16
```

## 3. 字段含义

### 3.1 Header 字段

| 字段 | 含义 | 本数据集取值 | 说明 |
|---|---|---|---|
| `m` | 武器平台数 = MARL Agent 数 | {3, 5, 10, 20} | 每个 Agent 独立决策 |
| `n` | 目标总数 | {10, 20, 50, 100} | 算法**不可见**（见 §7 信息边界） |
| `K` | 总决策时间步数 | 10（n≤50）/ 15（n=100） | |
| `mu` | 每平台**全程**初始总弹药 | max(1, min(K, ⌈0.3·n/m⌉)) | 跨时间步不恢复；区别于旧数据的"每波重置" |
| `dt` | 每时间步对应的现实秒数 | 2.0 s | 也是单步决策的天然实时阈值（⑦ 时延满足率基准） |
| `delta_d` | 目标每步逼近距离 | 1.0 km/步 | |
| `v_m` | 拦截弹飞行速度 | 1.5 km/s | 决定飞行步数 h |
| `pcap` | 有效杀伤概率上限（百分数） | 95（即 0.95） | 旧参数 `L`（固定射程窗口）已删除 |

### 3.2 Target Block：`k_arr w_j r0_j`

| 字段 | 含义 | 取值 | 说明 |
|---|---|---|---|
| `k_arr` | 目标首次被侦测的时间步 | [0, K)，均衡分布（每步 ⌊n/K⌋~⌈n/K⌉ 个） | t < k_arr 时任何算法不得获得该目标信息 |
| `w_j` | 目标价值/威胁权重 | 继承静态数据采样（~U{1..99}） | 来自 `50x100x1.txt` 子矩阵采样 |
| `r0_j` | 侦测时距防御边界距离 | U{5..15} km | 决定该目标的"动态交战窗口"长度 ⌈r0/delta_d⌉ 步 |

### 3.3 Probability Block：`i j p_ij`

武器 `i` 对目标 `j` 的**基础**杀伤概率，`p_ij ∈ (0,1)`，继承静态数据采样（~U(0,1)）。不同平台的 `p_ij` 不同 → Agent 异质性来源一。

### 3.4 Distance Block：`i j d0_ij`

目标 `j` 被侦测时平台 `i` 与其距离（整数 km）。生成式：`d0_ij = r0_j + b_i + eps_ij`（`b_i ~ U{0..3}` 平台位置差异，`eps_ij ~ U{0..2}` 扰动；两者仅用于生成，不写入文件），保证 `d0_ij ≥ r0_j`。不同平台的 `d_ij(t)` 不同 → Agent 异质性来源二。

## 4. 派生动力学（全部可由文件推导，无隐含参数）

`DNInstance` 提供：

| 量 | 公式 | 说明 |
|---|---|---|
| 边界距离 | `r_j(t) = max(0, r0_j − delta_d·(t − k_arr_j))` | `r=0` 且存活 → 突防 |
| 平台-目标距离 | `d_ij(t) = max(0, d0_ij − delta_d·(t − k_arr_j))` | |
| 有效杀伤概率 | `p_eff_ij(t) = min(pcap, p_ij·d0_ij / d_ij(t))` | 目标越近越高；到达时刻 = p_ij |
| 飞行时间 | `tau_ij(t) = d_ij(t)/v_m`（秒） | |
| 飞行步数 | `h = max(1, ⌈tau/dt⌉)` | 最少 1 步 |
| 结算时刻 | `t_hit = t + h` | 发射时锁定 `p_shot = p_eff_ij(t)`，之后不变 |
| 突防时刻 | `breakthrough_step(j) = k_arr_j + ⌈r0_j/delta_d⌉` | 该步及以后且存活 → 突防泄漏 |
| 威胁目标 | `breakthrough_step(j) < K` 的目标 | 回合内真正可能突防的子集 |

多枚拦截弹攻击同一目标独立结算；目标已被摧毁/已突防后到达的拦截弹记无效交战，弹药不返还（`数据集规则.md` §10）。

## 5. 数据集清单（32 个实例）

| 文件 | m | n | K | mu | 总弹药 | 总价值 | 威胁目标数 | 威胁价值占比% | 每步到达 |
|---|---|---|---|---|---|---|---|---|---|
| `dn_3x10_K10_s1.txt` | 3 | 10 | 10 | 1 | 3 | 560 | 2 | 19.8 | 1 |
| `dn_3x10_K10_s2.txt` | 3 | 10 | 10 | 1 | 3 | 556 | 1 | 6.3 | 1 |
| `dn_3x20_K10_s1.txt` | 3 | 20 | 10 | 2 | 6 | 1186 | 5 | 21.2 | 2 |
| `dn_3x20_K10_s2.txt` | 3 | 20 | 10 | 2 | 6 | 1045 | 1 | 1.5 | 2 |
| `dn_3x50_K10_s1.txt` | 3 | 50 | 10 | 5 | 15 | 2877 | 7 | 11.3 | 5 |
| `dn_3x50_K10_s2.txt` | 3 | 50 | 10 | 5 | 15 | 2804 | 5 | 7.6 | 5 |
| `dn_3x100_K15_s1.txt` | 3 | 100 | 15 | 10 | 30 | 5521 | 37 | 38.4 | 6-7 |
| `dn_3x100_K15_s2.txt` | 3 | 100 | 15 | 10 | 30 | 5521 | 25 | 24.2 | 6-7 |
| `dn_5x10_K10_s1.txt` | 5 | 10 | 10 | 1 | 5 | 560 | 2 | 19.8 | 1 |
| `dn_5x10_K10_s2.txt` | 5 | 10 | 10 | 1 | 5 | 556 | 1 | 6.3 | 1 |
| `dn_5x20_K10_s1.txt` | 5 | 20 | 10 | 2 | 10 | 1186 | 5 | 21.2 | 2 |
| `dn_5x20_K10_s2.txt` | 5 | 20 | 10 | 2 | 10 | 1045 | 1 | 1.5 | 2 |
| `dn_5x50_K10_s1.txt` | 5 | 50 | 10 | 3 | 15 | 2877 | 7 | 11.3 | 5 |
| `dn_5x50_K10_s2.txt` | 5 | 50 | 10 | 3 | 15 | 2804 | 5 | 7.6 | 5 |
| `dn_5x100_K15_s1.txt` | 5 | 100 | 15 | 6 | 30 | 5521 | 37 | 38.4 | 6-7 |
| `dn_5x100_K15_s2.txt` | 5 | 100 | 15 | 6 | 30 | 5521 | 25 | 24.2 | 6-7 |
| `dn_10x10_K10_s1.txt` | 10 | 10 | 10 | 1 | 10 | 560 | 2 | 19.8 | 1 |
| `dn_10x10_K10_s2.txt` | 10 | 10 | 10 | 1 | 10 | 556 | 1 | 6.3 | 1 |
| `dn_10x20_K10_s1.txt` | 10 | 20 | 10 | 1 | 10 | 1186 | 5 | 21.2 | 2 |
| `dn_10x20_K10_s2.txt` | 10 | 20 | 10 | 1 | 10 | 1045 | 1 | 1.5 | 2 |
| `dn_10x50_K10_s1.txt` | 10 | 50 | 10 | 2 | 20 | 2877 | 7 | 11.3 | 5 |
| `dn_10x50_K10_s2.txt` | 10 | 50 | 10 | 2 | 20 | 2804 | 5 | 7.6 | 5 |
| `dn_10x100_K15_s1.txt` | 10 | 100 | 15 | 3 | 30 | 5521 | 37 | 38.4 | 6-7 |
| `dn_10x100_K15_s2.txt` | 10 | 100 | 15 | 3 | 30 | 5521 | 25 | 24.2 | 6-7 |
| `dn_20x10_K10_s1.txt` | 20 | 10 | 10 | 1 | 20 | 560 | 2 | 19.8 | 1 |
| `dn_20x10_K10_s2.txt` | 20 | 10 | 10 | 1 | 20 | 556 | 1 | 6.3 | 1 |
| `dn_20x20_K10_s1.txt` | 20 | 20 | 10 | 1 | 20 | 1186 | 5 | 21.2 | 2 |
| `dn_20x20_K10_s2.txt` | 20 | 20 | 10 | 1 | 20 | 1045 | 1 | 1.5 | 2 |
| `dn_20x50_K10_s1.txt` | 20 | 50 | 10 | 1 | 20 | 2877 | 7 | 11.3 | 5 |
| `dn_20x50_K10_s2.txt` | 20 | 50 | 10 | 1 | 20 | 2804 | 5 | 7.6 | 5 |
| `dn_20x100_K15_s1.txt` | 20 | 100 | 15 | 2 | 40 | 5521 | 37 | 38.4 | 6-7 |
| `dn_20x100_K15_s2.txt` | 20 | 100 | 15 | 2 | 40 | 5521 | 25 | 24.2 | 6-7 |

**设计特征**：

- **难度随 m 递减、随 n/K 递增**：弹药/威胁比从 3×100 的 30 发 vs 37 威胁（紧张）到 20×100 的 40 发 vs 25 威胁（宽裕）；
- **受控对比**：同 `n` 同 `seed` 不同 `m` 的实例共享同一战场态势（相同 `k_arr`/`r0`/`w`/`p` 采样流），支持"平台数量"维度的受控实验；
- **威胁价值占比 1.5%~38.4%**：s1 普遍难于 s2，形成难度梯度；
- 晚到/远距离目标（`breakthrough_step ≥ K`）天然不构成威胁——算法必须自行从局部信息中识别真实威胁并分配弹药，这是数据集的核心区分度来源。

## 6. 生成规则与可复现性

| 项 | 规则 |
|---|---|
| 命令 | `python experiments/gen_dn_data.py --grid`（或 `--single --m --n --seed`） |
| seed 流 | 每实例 5 个独立 `RandomState(seed*100+1..5)` 分别控制：到达划分 / r0 / 平台偏移 b / 扰动 eps / w-p 采样 |
| w、p 继承 | 从 `data/50x100x1.txt` 无放回采样 m 行（武器）× n 列（目标）子矩阵；池不足时按同分布（w~U{1..99}，p~U(0,1)）合成。注：池文件已随旧静态算例清理（2026-08-25），现有 32 个实例生成于清理前；重新生成时将自动回退合成采样或用 `--pool` 另行指定 |
| k_arr | 均衡切分 + 随机置换派发（同 seed 完全可复现） |
| mu | `max(1, min(K, ⌈0.3·n/m⌉))`，与"拦截全部威胁所需弹药 ≈ 1.5×威胁数"基本持平 |
| 自检 | 每文件写入后重新解析断言：行数 = 1+n+2mn、k_arr∈[0,K) 均衡、d0≥r0、p∈(0,1)、块完整性 |

## 7. 信息边界（数据集存放全局信息，算法取局部信息）

**完整 `.txt` 仅由仿真环境读取。** Agent `i` 在时间步 `t` 可得（经 `get_observation(i,t)`）：

- 共享目标信息：当前 t、已出现目标 ID、`w_j`、`r_j(t)`、存活/摧毁状态、已出现时长；
- 自身信息：`B_i(t)`、自身 `p_ij`、自身 `d_ij(t)`、自身在途拦截弹状态；
- 公共环境知识（已确认）：`K / dt / delta_d / v_m / pcap / m`（平台总数）。

不可得：未出现目标的一切信息、未来 `k_arr`、`n`（目标总数）、其他 Agent 的 `p/d/弹药/动作/在途弹`、某目标当前被几枚拦截弹攻击。

## 8. 评价指标兼容性确认（对照《评价指标体系_精简三层版》）

**结论：表 1、表 2 共 16 项指标全部可在新数据集上实现；其中 4 项需口径适配，1 项需解释注意，另建议新增 2 项 DN 专属可选指标。**

### 8.1 表 1：通用核心指标（8 项）

| # | 指标 | 结论 | 口径适配 |
|---|---|---|---|
| ① | 泄漏率 | ✓ 适配 | 分子 = **突防价值**（已确认口径：期末存活目标不计泄漏），分母 = 总价值不变；附突防目标数 |
| ② | 全期期望代价 | ✓ 适配 | `cost_t = Σ_{j 在 t 步被射击} w_j·Π_{t 步发射的(i,j)}(1−p_shot_ij)`，`p_shot` 为发射时锁定值（需在结果结构中记录）；全期 = 逐步均值 |
| ③ | 最优间隙 | ✓ 注意 | 参考解改为"集中式当前状态 CPLEX 上界"（规范 §13）。**信息不对称**：参考解拥有联合完整状态，分布式算法的 gap 含信息代价；负 gap（利用延迟动力学超越近视最优）更可能出现且有意义 |
| ④ | 弹药效率 | ✓ 增强 | 公式不变；mu 变为全程总额后此指标从"次要"升为"关键"（弹药紧张） |
| ⑤ | 平均交战年龄 | ✓ 适配 | 定义不变（首次交战时的 age）；但"窗口"从固定 L=3 波变为每目标动态窗口 `⌈r0_j/delta_d⌉` 步 |
| ⑥ | 窗口覆盖率 | ✓ 适配 | 改为"**威胁目标**（breakthrough_step<K）中突防前至少被交战一次的比例"；分母从 n 改为威胁目标数（非威胁目标无需覆盖） |
| ⑦ | 决策时延 | ✓ 适配 | 从"每波"改为"每时间步"（多智能体取各 Agent 决策+同步的总耗时）；**dt=2s 提供天然实时阈值**，"60s 满足率"改为"dt 满足率"（单步决策 ≤ dt 秒） |
| ⑧ | 成功率 | ✓ 适配 | 每 Agent 每步合法动作占比；失败约定：非法/无动作 → 视为不发射（弹药保留），该步该 Agent 记失败 |

### 8.2 表 2：方法特定指标

| # | 指标 | 结论 | 说明 |
|---|---|---|---|
| ⑨ | 样本效率与收敛性 | ✓ 不变 | 训练侧指标，数据集无关 |
| ⑩ | 泛化性 | ✓ 增强 | 32 实例网格支持跨规模/跨 seed 零样本迁移；同 n 同 seed 不同 m 共享战场，支持受控迁移实验 |
| ⑪ | 训练成本 | ✓ 不变 | |
| ⑫ | Token 消耗 | ✓ 适配 | "每波"改"每时间步"；审计源 `logs/llm_calls.jsonl` 沿用 |
| ⑬ | LLM 时延开销 | ✓ | Δ⑦ = 带 LLM − 基线，按步计 |
| ⑭ | 建议采纳率 | ✓ 适配 | 模块语义需按多智能体角色重定义（如 LLM 目标优先级建议 vs Agent 实际动作的一致率） |
| ⑮ | 降级率 | ✓ 不变 | 降级兜底 + 审计机制沿用 |
| ⑯ | 增量收益 | ✓ 不变 | 同 seed 配对 + Wilcoxon |

### 8.3 建议新增（DN v1 专属，可选）

| # | 指标 | 定义 | 价值 |
|---|---|---|---|
| ⑰ | 无效交战率 | 发射后在途弹因"目标已被其他弹摧毁/已突防"而失效的比例 | 直接度量隐藏信息下的协同浪费（重复瞄准），是 DN v1 多 Agent 机制的独有诊断量 |
| ⑱ | 弹药轨迹 | 弹药耗尽时刻分布 / 期末剩余弹药 | 资源管理维度的补充诊断 |

### 8.4 统计协议沿用

多种子 mean±std（附最坏值）、同 seed 配对比较、失败步唯一语义、QA 门（同参数两次运行 result_hash 一致）全部沿用；最小结果文件结构需扩展为多智能体版本（见《DN-WTA_v1_下一步工作规划》§3.4）。

## 9. 已确认的设计决策记录

| 决策点 | 结论 |
|---|---|
| 算例规模 | MARL 网格 m∈{3,5,10,20} × n∈{10,20,50,100} × 2 seeds |
| 参数策略 | 单一默认组：dt=2s, delta_d=1km/步, v_m=1.5km/s, pcap=95, K=10/15；mu 重新生成 ⌈0.3n/m⌉ |
| 终点口径 | 不约束 r0 收敛；期末存活目标**不计泄漏**，只统计突防价值 |
| 公共知识 | K/dt/delta_d/v_m/pcap/m 对所有算法可见；n 与未来目标不可见 |
