# 动态 WTA 评价指标体系

---

## 0. 算法对比表实例（数值为随机占位，仅示意总览效果）

**入表标准**：第一层 8 项全算法同口径 → 进主表；⑨收敛样本为 RL 代表列、⑫Token 为 LLM 代表列，
以「—」占位保持单表总览；⑩⑪⑬–⑯ 拆入补充表。逐波曲线、Pareto 图不入表。

### 表 1 主表：通用核心指标（实例 dyn_50x100x3，seeds 42–44，mean±std）

| 算法 | 泄漏率% ↓ | 全期期望代价(×10⁻²) ↓ | 最优间隙% ↓ | 弹药效率(价值/发) ↑ | 交战年龄(波) | 窗口覆盖% ↑ | 时延P90(s) ↓ | 成功率% ↑ | 收敛样本 ↓ | Token/波(k) ↓ |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 规划器(CPLEX) | **0.00**±0.00 | 0.79 | 0.0 | 3.91 | 0.00 | **100** | 61.4 | 100 | — | — |
| 启发式 | **0.00**±0.00 | 1.04 | 31.6 | 3.80 | 0.00 | **100** | 0.80 | 100 | — | — |
| 贪婪 | **0.00**±0.00 | 1.42 | 79.7 | 3.67 | 0.00 | **100** | **0.02** | 100 | — | — |
| PPO | **0.00**±0.00 | 1.01 | 27.8 | 4.21 | 0.32 | 99.5 | 0.35 | 100 | 8.5M | — |
| MAPPO | **0.00**±0.00 | 0.88 | 11.4 | 4.88 | 0.58 | 99.8 | 0.52 | 100 | **3.2M** | — |
| MAPPO+LLM | 0.09±0.17 | **0.58** | **−26.6*** | **5.80** | 0.95 | 99.2 | 108.5 | 100 | **3.2M**† | 8.6 |

注：* 间隙以逐波 CPLEX 为参考，负值源于跨波延迟利用了逐波最优看不到的逼近动力学；
† 复用 MAPPO 同一权重，无额外训练；粗体为列最优。LLM 失败自动降级回退不计入失败（降级率见补充表）。

### 表 2 补充表：方法特定指标（只列适用算法）

| 指标 | PPO | MAPPO | MAPPO+LLM |
|---|---:|---:|---:|
| 训练墙钟 (h) | 6.5 | 9.8 | 9.8 |
| 零样本间隙（跨实例, %） | 38.2 | 21.5 | 19.8 |
| LLM 时延开销 (Δs/波) | — | — | +46.2 |
| 建议采纳率 (%) | — | — | 61.3 |
| 降级率 (%) | — | — | 4.2 |
| Δ期望代价 vs 同基 (配对, %) | — | — | −34.1** |

### 表格指标讲解示例（MARL vs CPLEX）
① 泄漏率 — 期末没守住的价值占比，问题的第一目标。本实例火力冗余（1500 发 vs 100 目标、每波仅 10 个新目标），CPLEX 已零泄漏，该指标在此实例饱和，区分不了两者——这正是②被设为主排名轴的原因。MARL 唯一可能输在这里：策略带随机性 + 若学会延迟交战，个别种子下可能出现末窗未摧毁→突防（所以推测为“0~0.1，附非零 std”，尾部风险略高于确定性求解）。

② 全期期望代价 — 用同一解析公式评估两者的分配，剥离 BDA 运气，量“决策本身好不好”。这是唯一可能出现反直觉结果的指标：

CPLEX 是逐波最优但结构上近视：它每波独立求解“当波期望存活价值最小”，把目标留到下波会严格增加当波目标值，所以它永远不会延迟（交战年龄恒 0）。
但本问题有逼近动力学：目标每波近 1 km，p_eff = min(0.95, p·d0/d) 随等待上升。跨波看，适度等待远目标反而降低总代价——你的 E4（LLM 延迟模块）实测降 33% 就是证据。
MARL 若按全期回报训练，有机会学到这种前瞻，总代价压到 0.79 以下（gap 为负）；若只学到逐波模仿，则落在 0.79~1.0（gap 为正）。这一列直接回答“MARL 学到的是策略还是复制品”。
③ 最优间隙 — 相对当波 CPLEX 最优的相对误差。CPLEX 自己是参考解，恒 0。MARL 的正 gap 来源：函数逼近误差、部分可观测（每波只见局部态势）、多智能体训练的非平稳性/信用分配难题、探索噪声。这是 MARL 的“学费”：判断标准不是“打平”，而是收敛到可接受带（如 ≤5%）。

④ 弹药效率 — 每发弹药换多少期望摧毁价值。CPLEX 在火力冗余下倾向“全打满”：对存活概率已很低的目标继续堆弹（过度杀伤），且在目标到达波就开火（距离远、p_eff 低，单发期望价值低）。MARL 学出的策略通常更“抠”：把弹留给近距高价值目标、少打废弹，故单发效率更高。质量相近时，这一列说明谁更精打细算——换到弹药紧张的难实例时它就是生死线。

⑤ 交战年龄 — 目标到达后平均等了几波才首次被打（0=即打，2=末窗压线）。CPLEX 恒 0（上面解释的结构性近视，不是缺点是“体制”）。MARL 若 >0，说明它利用了“等目标靠近再打命中率更高”的动力学——这是动态 WTA 与静态 WTA 的本质区别所在，也是 MARL 相对逐波求解器的理论增量来源。但注意与⑥联动：等待有窗口风险。

⑥ 窗口覆盖率 — L=3 窗口内至少被交战一次的目标比例，延迟策略的红线。CPLEX 全打满 → 100%。MARL 若延迟激进而探索噪声又在末窗失手，会掉到 99.x%，对应①里那零点几的泄漏。⑤⑥合起来读：年龄↑是收益（打得更准），覆盖↓是代价（偶尔错过）——两列共同刻画 MARL 的时机权衡。

⑦ 时延 P90 — 单波决策耗时的尾部分位。CPLEX 受 60s 时限 + 冷启动搜索 + subprocess 开销支配，P90 贴着 ~60s；MARL 是一次神经网络前向，几十毫秒级。这是 MARL 最硬的卖点（2~3 个数量级），也是论文里 Pareto 图的横轴：CPLEX 在左上（慢而准），MARL 在右下偏左（快而略糙）。实战含义：来袭节奏快于分钟级时，CPLEX 根本不可部署。

⑧ 成功率 — 成功给出合法分配的波次比例。CPLEX 实测 30/30=100%，但原理上有风险（时限内无可行解、社区版 1016 变量限制），失败波按协议“当波不打、目标滞留”——代价惨重。MARL 前向传播几乎不会失败，但需防数值异常（NaN、非法动作被掩码后仍输出空分配）。两者在此实例都应报 100%，此列是保险条款而非区分项：难实例上它才开始分化。

一句话总结两算法的根本差异
CPLEX：精确但近视且慢 —— 每波内最优（gap=0），但波间无前瞻（年龄=0、弹药效率被距离拖低），且 60s/波不可实时化；
MARL：近似但可前瞻且快 —— 付出 5~30% 的当波间隙作学费，换来 (a) 毫秒级推理，(b) 唯一可能吃到“跨波延迟收益”的架构（若奖励设计对，②可低于 CPLEX），(c) 更省弹。
---

## 1. 三层总览

| 层 | 定位 | 数 | 对象 |
|---|---|---|---|
| 一：通用核心 | WTA 本质重要 + 仅需标准输出（每波 assignment/solved/wall_time） | 8 | 全部算法同口径 |
| 二：RL 方法 | 训练侧可行且必需 | 3 | MARL 及其它 RL |
| 三：LLM 融合 | 量化 LLM 结合的代价与收益 | 5 | +LLM 的方法 |

核心逻辑：**期望层（决策好不好：②③）与实现层（实际结果：①⑧）分离互验**；资源（④）与时机（⑤⑥）刻画怎么打赢；时延与成功率（⑦⑧）刻画能不能用。

---

## 2. 第一层：通用核心指标（8 项）

所有指标取 Monte-Carlo 多种子 **mean±std（附最坏值）**。

| # | 指标 | 定义/公式 | 来源 | 说明 |
|---|---|---|---|---|
| ① | 泄漏率（主KPI） | (期末存活价值+全部突防价值)/5521 | `leak_rate` | 问题第一目标；附突防目标数(原A2)。本实例基线饱和→排名切换为②③；难实例恢复主位 |
| ② | 期望代价（逐波+全期）★ | `cost_k=Σ_j w_j·Π_i(1−p_eff_ij^k)^x_ij`；全期=均值 | 解析重算，与 `expected_cost` 等价 | 剥离随机性的决策质量核心轴（实测拉开33%差距），泄漏饱和时的主排名依据 |
| ③ | 最优间隙 | `gap_k=(cost_k−cost_k^best)/cost_k^best`，报 mean/max | ②+当波最优(规划器) | 近似/学习方法的纯决策误差，判断"够好"的标尺(如收敛带≤5%) |
| ④ | 弹药效率 | `Σ_k 期望摧毁_k / Σ_k Σ x_ij`，附总耗弹(耗弹率) | assignment+数据 | WTA 资源维度；质量相同时谁省弹，过度杀伤直接拉低此值 |
| ⑤ | 平均交战年龄 | `Σ_j 首次交战age_j / 曾交战目标数`(0=即打,2=末窗) | assignment+波次 | 动态时机维度：利用"逼近命中率升"动力学的能力；含原D1/D3/D4/D6信息 |
| ⑥ | 窗口覆盖率 | `#(L=3窗口内至少交战一次的目标)/100` | assignment | 红线：<1 即有目标被完全错过→必然突防；延迟类策略安全边界 |
| ⑦ | 决策时延 | 单波 wall_time 的 P50/P90/max + 60s满足率 | `wall_time` | 动态实时硬约束；五类算法差异最大维度(毫秒~100s+)，盒线图可比 |
| ⑧ | 求解/推理成功率 | `#成功给出分配的波 / K` | `solved` | 稳定性；失败波按协议记为保留全部当波目标进入下波（唯一失败约定） |

标准输出契约：每波 `{assignment(x_ij), solved(bool), wall_time(s)}`——任何算法（规划器/启发式/MARL/LLM 混合）只要产出该格式，第一层 8 项全部自动可算。

---

## 3. 第二层：RL 方法指标（3 项）

| # | 指标 | 定义 | 说明 |
|---|---|---|---|
| ⑨ | 样本效率与收敛性 | 训练曲线(回合 vs ③间隙 或 ②代价)；达到 gap≤5% 所需回合数/转移数 | 学习方法核心问题：多快学到多好；曲线附稳态波动 |
| ⑩ | 泛化性 | 零样本迁移到 `data/` 其他实例(wta_100x200x5、100x100x2、dyn 变体等)上的 ①②③ | 学到的是策略还是记忆；同一训练预算跨实例测 |
| ⑪ | 训练成本 | 总训练墙钟时间、网络参数量、(可选)峰值显存 | 可行性声明的一部分（实验楼算力约束下必需） |

（RL 推理侧的时延/成功率/稳定性已由第一层 ⑦⑧ 及多种子口径覆盖，不重复设。）

---

## 4. 第三层：LLM 融合指标（5 项）

数据源：`logs/llm_calls.jsonl`（每条含 phase/tokens/duration/finish_reason/错误）+ `run_rec["llm"]` 采纳审计。

| # | 指标 | 定义 | 说明 |
|---|---|---|---|
| ⑫ | Token 消耗 | 每波 prompt/completion tokens（pre/post 分列）、全期总量与均值 | LLM 成本的第一口径，随规模外推的依据 |
| ⑬ | LLM 时延开销 | 每波 LLM 调用时长(含重试)；增量时延 Δ⑦=E7(带LLM)−E7(基线) | 归因拆分：总时延里 LLM 占多少（实测单波约+45~55s） |
| ⑭ | 建议采纳率 | LLM 建议进入最终解的比例：热启动被 CPX 采用率 / 参数通过白名单率 / 被剔除目标实际未交战率 | LLM 判断的"命中率"，分模块报告 |
| ⑮ | 降级率 | LLM 调用失败(超时/格式/越界)回退纯算法路径的比例 | 融合系统的可靠性红线；降级波单独统计质量 |
| ⑯ | 增量收益 | 配对同 seed：Δ②(全期期望代价)、Δ①(泄漏率)、Δ⑦(时延) 及显著性(Wilcoxon) | "值不值"的总账：质量收益 vs 时延/Token 成本，Pareto 图佐证 |

（原 G10 可解释性作为定性附录保留优秀波次中文解释样例，不计分。）

---

## 5. 统一报告口径与协议

1. 多种子：seeds 42/43/44（BDA 随机流逐位可复现），每指标报 mean±std 附最坏值（原F1/F2）。
2. 配对比较：同 seed 下方法间配对差 + Wilcoxon 符号秩检验（n=30 波/种子可增样本）。
3. 失败波约定：失败波按 ⑧ 记失败，当波目标全部保留进入下波（唯一失败语义，全算法一致）。
4. QA 门（原F4，协议项非指标）：同参数两次运行 result_hash 必须一致，方可采信。
5. 汇总呈现：主表 = 16 项指标×方法矩阵；核心图 = 时延⑦×全期期望代价② 的 Pareto 散点（一图定胜负）。

## 6. 最小结果文件结构（标准输出格式）

```json
{
  "meta": {"instance": "...", "m": 50, "n": 100, "K": 10, "mu": 3, "L": 3, "pcap": 0.95, "total_value": 5521},
  "waves": [{
    "wave": 0,
    "assignment": {"i": 0, "j": 3, "shots": 2},
    "solved": true,
    "wall_time": 60.5,
    "expected_cost": 123.4,
    "first_engagement_age": {"3": 0},
    "llm": {"calls": 2, "tokens_prompt": 8000, "tokens_completion": 500,
            "duration_s": 45.0, "accepted": true, "degraded": false}
  }],
  "summary": {"leak_rate": null, "breakthrough_count": null, "shots_total": null,
               "coverage": null, "mean_engagement_age": null}
}
```

任何算法只要产出该结构，第一、三层指标全部自动可算；第二层指标由训练日志产出。

---

## 附：与原体系映射速查

精简后 ①=A1(+A2) ②=B1(+B2) ③=B3 ④=C2(+C1) ⑤=D2(+D1/D3/D4/D6) ⑥=D5 ⑦=E1(+E2) ⑧=E4；
⑨=G1+G2 ⑩=G3 ⑪=G4；⑫⑬=G5拆分 ⑭=G6 ⑮=G7 ⑯=G8+G9；F1/F2→报告口径，F4→QA门，
E5→Pareto汇总图，A4/D7→可选诊断图，G10→定性附录；删除 A3/A5/B4/C3/C4/E3/F3。
