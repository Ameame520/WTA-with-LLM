# 项目整理规范（Skill）

> **用途**：本项目以后任何代码重构/新增文件，都必须先阅读并遵循本规范。
> **制定日期**：2026-08-22（首次大规模重构时确立）。

## 一、目录总准则

**一个顶层子文件夹 = 一个模块的实现；任何文件夹下不再有子文件夹。**
仅两处例外：

1. `output/` 允许**一层**实验子文件夹（每次实验一个，内部不再嵌套）；
2. `data/` 中的数据文件直接平铺。

## 二、目录职责（冻结结构）

```
WTA-Dn-branch01/
├── main.py               # 全流程运行入口（唯一入口，从项目根目录运行）
├── README.md             # 项目说明（随结构变更同步更新）
├── 项目整理规范.md        # 本文件
├── 实验综合报告.md 等总结md  # 总结性质文档，一律放主目录
├── cplex/                # 模块①静态 WTA 精确求解器（原 src/wta_cplex.py, src/validator.py）
│                          # 注意：本目录【不加】__init__.py，避免遮蔽官方 cplex 包
├── dwta/                 # 模块②动态多波次仿真核心（instance / simulator / wave_runner）
├── llm/                  # 模块③LLM 辅助决策（llm_agent）
├── experiments/          # 模块④实验支撑（gen_dynamic_data 数据生成, report 报告生成）
├── data/                 # 算例数据（静态 .txt 与生成的动态 .txt）
├── logs/                 # 正式实验终端日志 + LOGS_SUMMARY.md（日志索引）
└── output/               # 每次实验一个单层子文件夹 + README.md（结果索引）
```

## 三、模块间依赖（保持单向）

```
main.py → dwta / llm / experiments / cplex(经子进程)
experiments/gen_dynamic_data → dwta.instance
experiments/report → （仅标准库，被 main.py 调用）
dwta/simulator → dwta/wave_runner → cplex/validator（sys.path 注入）+ cplex/wta_cplex.py（子进程）
llm/llm_agent → dwta.wave_runner（函数内延迟导入）
```

**禁止**：`cplex/` 反向 import 其他模块；模块间循环依赖；在 `cplex/` 放 `__init__.py`。

## 四、改动红线

1. **算法零改动原则**：重构只允许动"组织结构"（文件位置、import、路径字符串、文档），不许改任何算法逻辑、求解流程、数值参数；
2. 运行方式约定：**始终从项目根目录执行**（`python main.py ...`），相对路径 `data/`、`output/`、`logs/` 依赖 cwd；
3. 任何涉及 `src/` 的历史引用（文档、md5 键名、路径拼接）在重构时必须同步更新为上表新路径。

## 五、logs 与 output 约定

- `logs/`：只留正式实验的终端日志，命名 `<实验名>_run.log`；调试/冒烟日志用后即删；每新增正式日志要更新 `logs/LOGS_SUMMARY.md`；
- `output/`：每次正式实验建 `e<序号>_<标识>/` 文件夹，内放 `report.json / report.md / llm_calls.jsonl`；新实验要更新 `output/README.md` 对照表；
- 波次中间文件（`tmp/wave_*.json`）由 `main.py` 自动清理，不得手工留存。

## 六、重构后必做验证

```bash
cd <项目根>
python -c "import main"                          # 导入链完整
python cplex/wta_cplex.py --help                 # 求解器可独立运行
python main.py --smoke --seeds 3 --timelimit 30  # 端到端冒烟（不需要 LLM API）
```

三项全过，重构才算完成。

## 七、批量删除安全规则

**一次删除的文件不要超过 40 个**，否则会触发 IDE 的安全审查规则，删除被拦截。
需要批量清理（如 tmp 波次临时文件、过期日志、大批旧报告）时，请**分批删除**，每批控制在 40 个文件以内；大批量目录可先移到项目外的临时回收目录（如 `/tmp/`），确认项目无误后再统一清空。
