# 项目整理规范（Skill）

> **用途**：本项目以后任何代码重构/新增文件，都必须先阅读并遵循本规范。
> **制定日期**：2026-08-22（首次大规模重构时确立）；2026-09-01 随 branch01→branch02 迁移同步更新。

## 一、目录总准则

**一个顶层子文件夹 = 一个模块的实现；任何文件夹下不再有子文件夹。**
仅两处例外：

1. `output/` 允许**一层**实验子文件夹（每次实验一个，内部不再嵌套）；
2. `data/` 中的数据文件允许一层子文件夹，存放之前版本的实验数据，最新的直接用于实验的数据直接平铺。

## 二、目录职责（冻结结构）

```
WTA-Dn-branch02/
├── main.py               # 全流程运行入口（唯一入口，从项目根目录运行；DN-WTA 管线）
├── README.md             # 项目说明（随结构变更同步更新）
├── 项目整理规范.md        # 本文件
├── 数据集/指标等规范md    # DN-WTA_v2/v3_数据集说明.md、评价指标体系_精简三层版.md，一律放主目录
├── cplex/                # 模块①静态 WTA 精确求解器（wta_cplex.py + validator.py；仅作基线/参考，MARL 不依赖）
│                          # 注意：本目录【不加】__init__.py，避免遮蔽官方 cplex 包
├── dwta/                 # 模块②核心框架（dn_instance 数据层 / dn_env 多智能体时序环境 /
│                          #   dn_policies 基线策略 / wave_runner 求解器子进程机制）
├── experiments/          # 模块③实验支撑（gen_dn_data 数据生成 / dn_family_eval split 批量评估 /
│                          #   dn_report 指标报告）
├── data/                 # 算例数据（dn-data-v3 主基准 + 遗留版本数据子文件夹）
├── logs/                 # 正式实验终端日志（+ LOGS_SUMMARY.md 日志索引，随正式实验建立）
└── output/               # 每次实验一个单层子文件夹 + README.md（结果索引，随正式实验建立）
```

MARL 算法模块（如 `marl/`）未来新增时，同样遵循"一个顶层文件夹 = 一个模块"。

## 三、模块间依赖（保持单向）

```
main.py → dwta / experiments / cplex(经子进程)
experiments/gen_dn_data    → dwta.dn_instance
experiments/dn_family_eval → dwta.dn_env / dwta.dn_policies / dwta.wave_runner / experiments.dn_report
dwta/dn_policies → dwta/wave_runner → cplex/validator（sys.path 注入）+ cplex/wta_cplex.py（子进程）
（未来）marl/* → dwta/dn_env（环境接口）、dwta/dn_policies（策略接口）
```

**禁止**：`cplex/` 反向 import 其他模块；模块间循环依赖；在 `cplex/` 放 `__init__.py`。

## 四、改动红线

1. **算法零改动原则**：重构只允许动"组织结构"（文件位置、import、路径字符串、文档），不许改任何算法逻辑、求解流程、数值参数；
2. 运行方式约定：**始终从项目根目录执行**（`python main.py ...`），相对路径 `data/`、`output/`、`logs/` 依赖 cwd；
3. 任何涉及历史路径的引用（文档、md5 键名、路径拼接）在重构时必须同步更新为本表新路径。

## 五、logs 与 output 约定

- `logs/`：只留正式实验的终端日志，命名 `<实验名>_run.log`；调试/冒烟日志用后即删；每新增正式日志要更新 `logs/LOGS_SUMMARY.md`；
- `output/`：每次正式实验建 `e<序号>_<标识>/` 文件夹，内放 `report.json / report.md`（族评估为 `family_report.*`）；新实验要更新 `output/README.md` 对照表；
- 求解器临时文件（`output/tmp/dn_t*`）由 `main.py` 自动清理，不得手工留存。

## 六、重构后必做验证

```bash
cd <项目根>
python -c "import main"                          # 导入链完整
python cplex/wta_cplex.py --help                 # 求解器可独立运行（需 cplex 环境）
python main.py --instance data/dy-data-v1/dn_3x10_K10_s1.txt \
    --policy greedy --seeds 3                    # 端到端冒烟（不需要 CPLEX）
```

三项全过，重构才算完成。

## 七、批量删除安全规则

**一次删除的文件不要超过 40 个**，否则会触发 IDE 的安全审查规则，删除被拦截。
需要批量清理（如求解器临时文件、过期日志、大批旧报告）时，请**分批删除**，每批控制在 40 个文件以内；大批量目录可先移到项目外的临时回收目录（如 `/tmp/`），确认项目无误后再统一清空。
