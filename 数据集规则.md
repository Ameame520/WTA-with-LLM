# DN-WTA v1 数据集与环境规范

## 1. 设计目标

DN-WTA v1 用于描述高动态武器目标分配问题，在原始静态 WTA 数据基础上引入：

1. 目标分时到达；
2. 目标持续逼近；
3. 杀伤概率随距离动态变化；
4. 武器平台跨时间步共享总弹药约束；
5. 拦截弹飞行时间与延迟命中反馈；
6. 不同平台具有不同的 `p_ij` 和 `d_ij`；
7. 完整环境数据与算法可观测信息分离；
8. 多 Agent 局部决策与时间步末信息同步。

数据文件继续采用纯 `.txt` 数值格式。

---

## 2. 数据文件结构

完整格式：

```text
[Header]
m n K mu dt delta_d v_m pcap

[Target Block] × n
k_arr w_j r0_j

[Probability Block] × m*n
i j p_ij

[Distance Block] × m*n
i j d0_ij
```

总行数：

```text
1 + n + 2*m*n
```

---

## 3. Header 字段

```text
m n K mu dt delta_d v_m pcap
```

| 字段        | 含义                        |
| --------- | ------------------------- |
| `m`       | 武器平台数量，同时对应 MARL Agent 数量 |
| `n`       | 目标总数                      |
| `K`       | 总决策时间步数                   |
| `mu`      | 每个武器平台在整个任务周期内的初始总弹药量     |
| `dt`      | 每个 time-step 对应的现实时间间隔 Δt |
| `delta_d` | 目标每个 time-step 向防御区域逼近的距离 |
| `v_m`     | 防御方拦截弹飞行速度                |
| `pcap`    | 有效杀伤概率上限，百分数表示            |

例如：

```text
3 10 10 3 2.0 1.0 1.5 95
```

表示 3 个武器平台、10 个目标、10 个决策时间步，每个平台整个任务周期共有 3 发弹药，每步 2 秒，目标每步逼近 1 km，拦截弹速度为 1.5 km/s，有效杀伤概率最高为 0.95。

原有固定射程参数 `L` 删除。目标一旦被侦测，即使距离较远也允许交战。

---

## 4. Target Block

每个目标一行：

```text
k_arr w_j r0_j
```

### `k_arr`

目标首次被侦测并进入系统的时间步：

[
k_{arr,j}\in[0,K)
]

完整 `.txt` 中保存所有目标的 `k_arr`，但在：

[
t<k_{arr,j}
]

时，任何算法均不得获得该目标的信息。

### `w_j`

目标价值或威胁权重。

原则上直接继承原始静态 WTA 数据中的目标权重，不重新生成。

### `r0_j`

目标首次被侦测时距离防御边界的距离。

目标距离随时间演化：

[
r_j(t)=\max\left(0,r_{0j}-\Delta d(t-k_{arr,j})\right)
]

当：

[
r_j(t)=0
]

且目标仍然存活时，判定目标突防。

---

## 5. Probability Block

格式保持原始 WTA 不变：

```text
i j p_ij
```

其中：

[
p_{ij}\in(0,1)
]

`p_ij` 表示武器平台 `i` 对目标 `j` 的基础杀伤概率，原则上直接继承原始静态 WTA 数据。

不同平台具有不同的 `p_ij`，构成 Agent 异质性的一个主要来源。

---

## 6. Distance Block

新增平台—目标初始距离矩阵：

```text
i j d0_ij
```

其中 `d0_ij` 表示目标 `j` 被侦测时，武器平台 `i` 与目标 `j` 之间的距离。

平台—目标距离随时间演化：

[
d_{ij}(t)=\max\left(0,d^0_{ij}-\Delta d(t-k_{arr,j})\right)
]

建议数据生成时满足：

[
d^0_{ij}\ge r^0_j
]

可采用：

[
d^0_{ij}=r^0_j+b_i+\epsilon_{ij}
]

其中 `b_i` 表示平台位置差异，`\epsilon_ij` 表示小幅随机扰动。`b_i` 和 `\epsilon_ij` 仅用于数据生成，不需要写入最终 `.txt`。

不同平台对应不同的 `d_ij`，构成 Agent 异质性的另一主要来源。

---

## 7. 动态有效杀伤概率

有效杀伤概率由基础杀伤概率和当前平台—目标距离共同决定：

[
p^{eff}_{ij}(t)
===============

\min
\left(
p_{cap},
p_{ij}\frac{d^0_{ij}}{d_{ij}(t)}
\right)
]

目标刚出现时：

[
p^{eff}*{ij}=p*{ij}
]

随着目标逼近：

[
d_{ij}(t)\downarrow
\Rightarrow
p^{eff}_{ij}(t)\uparrow
]

由此形成「提前交战概率较低」与「延后交战概率较高但突防风险增加」之间的动态权衡。

---

## 8. 跨时间步弹药约束

`mu` 不再表示每个时间步重新恢复的弹药容量，而表示每个平台整个任务周期的初始总弹药量。

设平台 `i` 的剩余弹药为：

[
B_i(t)
]

初始：

[
B_i(0)=\mu
]

每发射一枚拦截弹：

[
B_i(t^+)=B_i(t)-1
]

后续时间步不恢复弹药。

因此当前射击决策会直接影响后续阶段的可用资源。

---

## 9. 拦截弹飞行与延迟反馈

平台 `i` 在时间步 `t` 对目标 `j` 发射拦截弹时，首先根据当前状态锁定该枚拦截弹的杀伤概率：

[
p^{shot}=p^{eff}_{ij}(t)
]

发射后该概率不再变化。

飞行时间为：

[
\tau_{ij}(t)=\frac{d_{ij}(t)}{v_m}
]

对应的离散时间步数为：

[
h=
\max
\left(
1,
\left\lceil
\frac{\tau_{ij}(t)}{\Delta t}
\right\rceil
\right)
]

预计结算时间：

[
t_{hit}=t+h
]

拦截弹发射后进入「在途」状态，到达目标时才根据锁定的 `p_shot` 进行 Bernoulli 命中结算。

---

## 10. 多枚拦截弹结算规则

若多枚拦截弹攻击同一目标，各枚拦截弹独立进行概率结算。

若目标在某枚拦截弹到达前已经被其他拦截弹摧毁：

* 已发射弹药仍然消耗；
* 后续到达的拦截弹记为无效交战；
* 弹药不返还。

若目标已经突防，在途拦截弹同样失效。

---

## 11. 每个 time-step 的执行流程

每个时间步按照以下顺序执行：

### Step 1：目标出现

满足：

[
k_{arr,j}=t
]

的目标进入当前环境。

### Step 2：生成 Agent Observation

环境根据 Global State 为每个平台生成对应的局部观测：

[
O_i(t)
]

### Step 3：各 Agent 同时决策

每个平台根据自身 observation 独立选择动作。

DN-WTA v1 中，每个平台每个 time-step 最多发射 1 枚拦截弹，也可以选择不发射。

### Step 4：执行发射并扣除弹药

所有 Agent 的合法动作同时执行。

### Step 5：推进环境

时间推进 Δt，目标距离同步变化。

### Step 6：结算到达的拦截弹

所有达到 `t_hit` 的拦截弹进行命中结算。

### Step 7：突防判定

若目标距离防御边界：

[
r_j(t)\le0
]

且仍然存活，则判定为突防。

若拦截弹到达和目标到达防御边界发生在同一离散时间点，先进行拦截结算，再进行突防判定。

### Step 8：信息同步

更新所有目标的公共状态，各 Agent 在下一 time-step 使用更新后的状态重新决策。

---

## 12. Global Dataset、Global State 与 Observation

### 12.1 Global Dataset

完整 `.txt` 文件描述整个仿真环境，包括：

* 全部目标的 `k_arr`；
* 全部目标的 `w_j`；
* 全部目标的 `r0_j`；
* 完整 `p_ij`；
* 完整 `d0_ij`；
* 全部环境参数。

完整数据文件仅由仿真环境读取，不直接作为算法输入。

### 12.2 Global State

仿真器内部维护完整状态，例如：

* 当前 time-step；
* 已出现和未出现目标；
* 所有目标当前状态；
* 所有平台剩余弹药；
* 完整 `p_ij`；
* 完整 `d_ij(t)`；
* 所有在途拦截弹；
* 历史发射和结算结果。

Global State 代表完整环境状态，不等于任何算法实际能够获得的信息。

### 12.3 Agent Observation

Agent `i` 可以获得：

**共享目标信息：**

* 当前 time-step；
* 已出现目标 ID；
* 目标价值 `w_j`；
* 目标当前距离防御边界 `r_j(t)`；
* 目标当前存活/摧毁状态；
* 目标已经出现的时间长度。

**Agent-specific 信息：**

* 自身剩余弹药 `B_i(t)`；
* 自身对各目标的 `p_ij`；
* 自身与各目标的 `d_ij(t)`；
* 自身已经发射的在途拦截弹状态。

Agent `i` 不获得：

* 尚未出现目标的信息；
* 未来目标的 `k_arr`；
* 其他 Agent 的 `p_ij`；
* 其他 Agent 的 `d_ij`；
* 其他 Agent 的剩余弹药；
* 其他 Agent 当前动作；
* 其他 Agent 的历史发射记录；
* 其他 Agent 的在途拦截弹信息；
* 某目标当前共有多少枚拦截弹正在攻击。

各 Agent 在一个 time-step 内独立决策，time-step 结束后再同步目标是否被摧毁等公共状态。

---

## 13. 不同算法的统一信息条件

MARL、贪婪、启发式等分布式算法统一通过：

```text
get_observation(i, t)
```

获得 Agent `i` 当前允许使用的信息。

不得出现：

```text
MARL → Local Observation
Greedy / Heuristic → 直接读取完整 txt
```

主实验中的分布式算法应保持一致的信息边界。

### 分布式公平对比组

可包括：

```text
MARL
Decentralized Greedy
Decentralized Heuristic
其他局部策略
```

这些方法均根据各 Agent 的 Local Observation 独立决策。

### 集中式 CPLEX

原始 CPLEX 求解需要同时获得完整的当前 `p_ij`、`d_ij` 和资源状态，因此属于集中式方法。

CPLEX 可以获得**当前已经出现目标的完整联合状态**，但不得提前获得尚未出现目标的信息。

该方法应作为：

```text
Centralized Current-State Upper Bound
```

用于提供集中式规划参考，而不与 MARL 作为完全相同信息条件下的主公平对比。

---

## 14. MARL 信息使用原则

每个武器平台对应一个 Agent。

Agent 异质性主要来源于：

[
p_{ij}
]

和：

[
d_{ij}
]

即不同平台由于自身能力和相对位置不同，对同一目标具有不同的打击效果。

各 Agent 不进行完全信息隔离。目标价值、目标当前状态、目标是否被摧毁等信息可以共享，但不同平台自身的能力、距离、弹药以及发射状态保持局部。

若采用 CTDE 架构：

* 训练阶段 Critic 可以使用当前时刻多个 Agent 的联合状态；
* 执行阶段 Actor 只能使用自身 Observation；
* 训练和执行阶段均不得使用尚未出现目标的未来信息。

---

## 15. DN-WTA v1 核心动态特征

DN-WTA v1 相比原始静态 WTA 和当前基础 DWTA，主要增加以下动态机制：

1. **目标到达动态**：目标在不同时间步被侦测；
2. **距离动态**：目标持续向防御区域逼近；
3. **杀伤概率动态**：有效杀伤概率随平台—目标距离变化；
4. **资源动态**：弹药跨时间步持续消耗；
5. **行动延迟**：发射后需要经过飞行时间才能获得结果；
6. **反馈延迟**：在途期间无法确认当前射击是否成功；
7. **平台异质性**：不同 Agent 具有不同 `p_ij` 和 `d_ij`；
8. **信息动态**：未来目标不可提前观测；
9. **多 Agent 协同**：Agent 在同一时间步独立决策，并在时间步结束后同步公共结果。

上述机制在保持 `.txt` 数值数据结构简单的同时，使问题具备明显的跨时间资源耦合、延迟反馈和多平台局部协同特征。
