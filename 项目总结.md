项目总结：WTA-Dn-branch03
一、项目定位
动态武器-目标分配（DWTA）实验框架，包含三条管线：静态 WTA 的 CPLEX 精确求解器、动态多波次蒙特卡洛仿真、以及 LLM 辅助决策实验。入口统一为 main.py，共四个模块：cplex/（静态求解核心，未改动）、dwta/（仿真环境与策略）、llm/（LLM 决策代理）、experiments/（数据生成与报告）。

二、最新数据集：DN-WTA v2（2026-08-26 修订）
当前实验基准是 data/dn_3x50_K10_s1.txt 与 dn_3x50_K10_s2.txt（同规模跨 seed 对比组），建模3 个异质武器平台在观测受限下协同拦截分批到达的目标流：

要素	设定
规模	m=3 平台 × n=50 目标（10 波 × 每波 5 个）× K=10 步（dt=10s，全程 100s）
弹药	全局共享池 24 发（8 发/平台），全程不补给，每平台每步至多 1 发
动力学	目标每步逼近边界 3 km；命中概率随距离演化 p_eff = min(0.95, p_ij·d0/d(t))；拦截弹飞行 1~2 步后延迟伯努利结算
核心难度	24 发 vs 50 目标 → 防御天花板 < 50%，逼算法做“打谁/谁打/何时打”的取舍
信息边界	算法只能通过 get_observation(i,t) 取局部信息，不知目标总数、未来到达、其他平台火力
无防御时 50/50 全部突防（泄漏直方图 2,4,5,5,5,5,5,5,5 + 期末 9，逐窗压力均匀）。

三、运行在 v2 上的算法（dwta/dn_policies.py + dn_env.py）
三种策略形成“下界—被评测对象—上界”的对比结构：

none — 无防御基线，永不发射，标定泄漏上界（=1.0）；
greedy — 分布式局部贪心（被评测对象）：每个平台仅用本地观测，按 w_j × p_eff / 剩余窗口数 评分选目标，平台间无通信 → 会重复瞄准；
cplex — 集中式短视最优（上界基准）：每步构建 0-1 规划子问题，经子进程调用原 CPLEX 求解器求逐步全局最优，超池容量时按边际价值修剪；同时充当 greedy 的 gap 参考解。
四、最新实验结果（E5–E10，20 seeds × 2 实例 × 3 策略）
code
泄漏率   none 1.000  ≫  greedy 0.576/0.536  >  cplex 0.492/0.438   (s1/s2)
关键结论：

集中化协同价值约 8~10 个百分点泄漏率：分布式贪心因重复瞄准（无效交战率 22–28%，约 5–7 发浪费）与次优分配（gap 0.17/0.26）双重损失；
弹药是硬约束：两策略均 24 发打尽，cplex 已逼近短视可达下限，进一步提升需前瞻性弹药调度而非逐步优化；
实时性充裕：含 CPLEX 子进程的决策时延 P90 ≤ 0.46s ≪ 10s 门槛，满足率 100%；
确定性有 QA 保障：三轮重跑 result_hash 完全一致；s2 上发现 greedy 决策路径种子不变性（末窗交战机制），为后续改进提供了干净的诊断基线。